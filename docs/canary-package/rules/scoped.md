---
alwaysApply: false
applyTo: "docs/** — when reading or editing a file under docs"
---

# ACR canary scoped rule

ACR-CANARY-SCOPED-7f3a is the sentinel of the path-scoped canary rule.

It should reach the session only while a file under `docs/` is in scope. Quote
the sentinel when asked, and do nothing else.
