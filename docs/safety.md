# Command safety contract

ACR writes only project state, adapter output it can prove ownership of, and its own transaction metadata. The table below is the complete command surface; flag rows call out options that change the ownership or mutation boundary.

Three rules apply to every row:

1. A generated-only target is locally excluded through `.git/info/exclude`. `git checkout` cannot restore an untracked generated target; run `acr realize` to regenerate it.
2. A shared target is project source. ACR changes only its managed entries, emits `shared_file_requires_commit`, and never stages it. Commit shared files; `git checkout <path>` restores the committed version.
3. `--dry-run` prints the same plan and writes nothing, including no `agents.yaml`, `.agents/registry.lock`, transaction, vendor-tree, native-file, or Git-exclusion update.

## Command matrix

| Command | May create or overwrite | May remove | Never touches | Undo |
| --- | --- | --- | --- | --- |
| `acr init` | The `agents` and `freshness` fields in `agents.yaml`; a missing `.agents/registry.lock` needed for the two-file state transaction | No dependency, hold, lock row, artifact, or native output | Dependency declarations, holds, unknown `agents.yaml` fields, and all native files | Re-run `acr init` with the prior selections, or restore committed `agents.yaml` with `git checkout agents.yaml` |
| `acr init --agent NAME` | Replaces the stored agent selection with the repeated flag values | Agent IDs omitted from the replacement selection | Dependencies, holds, lock rows, and native output; run `acr realize` separately to change output | Re-run `acr init --agent ...` with the prior complete selection |
| `acr init --freshness POLICY` | Replaces the stored freshness policy | The selection of a future freshness hook when policy is `none` | Existing native hook files until `acr realize` runs | Re-run `acr init --freshness <old-policy>` and then `acr realize` |
| `acr init --non-interactive` | The same selection fields as `acr init`, using only stored or explicit choices | No additional paths | Terminal input and prompt output | Use the same undo as `acr init` |
| `acr install [SOURCE[@VERSION]]` | Dependency declarations and immutable lock rows in `agents.yaml` and `.agents/registry.lock`; first install may also store agent and freshness selections | A replaced lock resolution; no realized artifact | Native output and the Git index | Restore the two committed state files, or install the previous explicit reference and then run `acr realize` |
| `acr install --agent NAME` | First-install agent selection plus the normal dependency state | Agent IDs omitted from that first selection | Native output until `acr realize` runs | Restore the prior state files or re-run `acr init` with the prior selection |
| `acr install --freshness POLICY` | The stored freshness choice plus normal dependency state | A prior freshness choice | Native hook output until `acr realize` runs | Restore the prior state files or re-run `acr init --freshness <old-policy>` |
| `acr install --non-interactive` | The same files as install, without asking setup or downgrade questions | No additional paths | Terminal input and prompt output | Use the same undo as `acr install` |
| `acr install --hold` | A temporary hold in both the declaration and lock row, plus the known-good resolution | The prior lock resolution and any earlier hold in both files | Native output until `acr realize` runs | Run `acr resume SOURCE` to return to `latest`, or `acr install SOURCE@REF --pin` to make a fixed pin |
| `acr install --pin` | A fixed declaration and matching immutable lock row | A `latest` declaration and its hold in both `agents.yaml` and `.agents/registry.lock` | Native output until `acr realize` runs | Install `SOURCE@latest`, review the resolved version, then run `acr realize` |
| `acr realize` | Selected adapters' generated-only files, managed shared entries, the realization ledger, the ACR block in `.git/info/exclude`, and the owned freshness hook | Proven obsolete generated-only files; owned entries in shared files; a previously owned freshness hook when policy is `none` | Unmanaged bytes and entries, tracked whole files, the Git index, Tessl-owned paths, and unselected adapters' output | Restore committed shared files with `git checkout <path>`; restore state if needed; run `acr realize` to regenerate generated-only output |
| `acr realize --agent NAME` | Only selected adapters' output and ledger entries | Only obsolete output owned by the selected adapters | Every omitted adapter's files and ledger entries | Run `acr realize` with the project's full stored selection |
| `acr list` | Nothing | Nothing | Every project and machine-local file | No undo is needed; the command is read-only |
| `acr outdated` | Nothing | Nothing | Dependency state, archives, native output, and the freshness timer | No undo is needed; the command is read-only |
| `acr freshness run` | The machine-local throttle record; under stored policy `install`, eligible dependency state and an apply-mode realization | Under policy `install`, obsolete owned output; under `none`, nothing in the project | Explicit pins, held dependencies behind their barrier, and unmanaged content | Restore committed state/shared files and run `acr realize`; generated-only output is regenerated from the restored lock |
| `acr freshness run --policy POLICY` | The same paths as the selected policy for this invocation; `install` reconciles and realizes, `outdated` writes only throttle state, `none` writes nothing | `install` may remove obsolete owned output; other policies remove nothing | The stored `freshness` field in `agents.yaml` | Re-run with the intended policy; restore committed state if `install` changed it |
| `acr update [SOURCE]` | Eligible `latest` lock rows and declarations when their resolved evidence changes | Replaced lock resolutions | Explicit pins, holds that block the candidate, native output, and the Git index | Restore the committed state files, or install the previous explicit reference with `--hold` or `--pin`, then run `acr realize` |
| `acr resume SOURCE` | The newly resolved `latest` lock row and declaration | The rollback hold from both `agents.yaml` and `.agents/registry.lock` | Other dependencies and native output until `acr realize` runs | Reinstall the known-good `SOURCE@REF --hold`, then run `acr realize` |
| `acr uninstall SOURCE` | Pruned state, surviving packages' realized output, and the ACR block in `.git/info/exclude` | The declaration, its hold, the lock row and its hold; removed package entries; proven generated-only targets; its verified vendor tree after the state transaction | Unmanaged content, other packages' entries, user hooks, agent/freshness selections, machine-local freshness state, and bytes outside the ACR exclusion block | Reinstall the same source/reference and run `acr realize`; restore committed shared files if needed |
| `acr check` | Nothing | Nothing | Every project and machine-local file; it does not recover a pending journal | No undo is needed; exit `3` means safe unapplied changes and exit `4` means a refusal |
| `acr check --agent NAME` | Nothing | Nothing | Every file, including omitted adapters' state | No undo is needed; remove the flag to check the full stored selection |
| `acr publish [PATH]` | One GitHub draft followed by an immutable visible release and exactly three release assets | A same-tag draft only after its asset names and `acr-package.json` prove ACR ownership of this exact tagged tree | Local package bytes, tags, commits, Git index, visible releases, and foreign drafts | A visible release is immutable; bump `agent-plugin.yaml`, commit, tag, and run `acr publish` for a new version |
| `acr migrate tessl` | `agents.yaml`, `.agents/registry.lock`, and ACR-owned native paths and spans | Obsolete ACR-owned output only | `tessl.json`, `.tessl/**`, `tessl__*` native output, Tessl-managed host spans/objects, and the Tessl `.gitignore` block | Restore the committed state/shared files, remove ACR-only generated output through state plus `acr realize`, and keep Tessl installed |
| `acr migrate tessl --non-interactive` | The same coexistence state as `acr migrate tessl`, using explicit/default choices | No additional paths | Terminal input and prompt output | Use the same undo as `acr migrate tessl` |
| `acr migrate tessl --mapping-file PATH` | The same coexistence state, with mappings selected from the file below CLI mappings | Replaced ACR mapping-derived lock rows | The mapping file itself and all Tessl-owned content | Correct the mapping file and rerun `acr migrate tessl`, or restore committed ACR state |
| `acr migrate tessl --map FROM=SOURCE` | The same coexistence state, with the named CLI mapping taking precedence | A prior ACR mapping or an equivalent superseded vendor source | Tessl-owned content and unrelated mappings | Re-run with the prior mapping or restore committed ACR state |
| `acr migrate tessl --vendor-unmapped` | Verified regular files under `.agents/vendor/<workspace>/<package>`, vendor declarations/locks, and their realized output | A staged vendor tree on transaction failure; an equivalent prior vendor tree when an explicit mapping supersedes it | Symlinks, special files, unsafe paths, and the installed `.tessl/plugins/**` source tree | Run `acr uninstall vendor:<workspace>/<package>` or restore committed state and vendor files, then run `acr realize` |
| `acr migrate tessl --finalize` | Finalized state and re-anchored `OutputHash` values for every affected ledger target | Positively identified Tessl files and native artifacts; exact Tessl Markdown/config entries; the Tessl-managed `.gitignore` span; `tessl.json` last | Unsupported MCP configuration, unrelated siblings, unmanaged bytes, `.agents/`, untracked manifest/vendor evidence, and the printed `/.agents/*` plus `!/.agents/vendor/` remedy | Restore committed `tessl.json` and vendor files with `git checkout`, then run `tessl install`; restore other committed splices as needed |
| `acr migrate tessl-plugin [PATH]` | A new `agent-plugin.yaml` written atomically | A stale package-local `.agent-plugin.yaml.tmp` from a crashed prior conversion | Rule, skill, script, hook, Tessl manifest, README, and ignore-file bytes | Remove the new file, or restore committed `agent-plugin.yaml` with `git checkout agent-plugin.yaml` |
| `acr migrate tessl-plugin --repository URL` | The `source.repository` field in the new `agent-plugin.yaml` | Nothing beyond the stale conversion temporary file | Every existing source and Tessl manifest | Remove or restore `agent-plugin.yaml`, then retry with the prior URL |
| `acr migrate tessl-plugin --accept-agent-widening` | A new manifest whose converted native hook can run on every ACR adapter | Nothing beyond the stale conversion temporary file | Tessl hook definitions and artifact source files | Remove or restore `agent-plugin.yaml`; narrow the Tessl hook declaration before converting again |

## Rollback mechanisms

### Dependency hold barrier

`acr install SOURCE@REF --hold` keeps `requested: latest` while recording the known-good pin and rejected release in both state files. The barrier never clears automatically. `acr resume SOURCE` removes both hold records and resolves `latest`; `acr install SOURCE@REF --pin` removes both holds and leaves the dependency permanently fixed.

### Journal recovery

Mutating realization and migration operations claim the project and persist synced before-images before their first rename. A retry recovers a complete journal. Read-only commands report `pending_transaction`; a hash that matches neither before nor after reports `recovery_conflict`. Resolve the named file, then retry the original command. `transaction_busy`, `transaction_lock_unavailable`, `unsupported_journal_version`, and `stale_transaction_staging` never authorize an unproven write.

### Migration undo

Finalization requires tracked Tessl evidence so the original tool can be restored. Run `git checkout tessl.json .agents/vendor`, restore any committed native/config files named by the finalization report, then run `tessl install`. ACR never prints this as a substitute for the dependency hold barrier or journal recovery; they protect different boundaries.
