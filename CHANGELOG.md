# Changelog

## Next

### Added

- Add explicit Claude-assisted clean producer migration with tool-free structured proposals, validation before writes, portable skill metadata and preserved license support files. Report paid Tessl scoring removal separately from functional checks. Add `acr install --if-missing` for setup helpers that preserve existing dependency requests and holds.

- Add the deterministic foundation of clean producer conversion for #117: `acr migrate tessl-plugin --acr-only --repository URL [--package-version SEMVER]` derives target identity, retains nested content under a root manifest, rewrites proven owned skill references, retires selected producer metadata and translates the recognized standalone Tessl publisher to pinned ACR tag publication. Exact dry-run deltas, source/mode preconditions, exclusive writes, rollback diagnostics and a versioned nonpublished receipt support safe application and inert reruns. Default dual-distribution conversion remains compatible. Custom Tessl configuration/dependency operations, dynamic paths, review-policy changes and undistributed license files refuse before writes; untouched GOC positive acceptance and full issue #117 completion remain outstanding. Tests execute actual clean CLI output through publication, installation and all three adapters, preserving executable status without repairs.

- Finalize an intentionally changed replacement package through explicit, evidence-bound acceptance. Converting a real consumer off Tessl surfaced the gap: `acr migrate tessl --finalize` against `tessl-labs/good-oss-citizen@1.1.12` mapped to `github:jbaruch/good-oss-citizen` exited `4` with six `effective-diff` blockers and one `lossy-artifact` blocker whose detail was `description`, and every one of them was the conversion working as intended — the replacement rewrote five skills on purpose, and ACR's activation model has no field for a Tessl rule's `description`. The refusal was correct and had no exit: forcing byte equality would have meant editing the reviewed package back into Tessl's shape, and no flag existed to say "I read these differences and they are the point". A `--finalize --dry-run` preview now reports an `acceptance` bundle. Its `bindings[]` carry, per mapped package, the installed Tessl package's own effective digest and version alongside the replacement's source, requested ref, resolved commit and resolved content hash; its `changes[]` carry every reviewable difference with its reason and both digests; and `token` is the SHA-256 of that bundle behind an `acr-accept-1:` prefix. Re-running with `--accept-reviewed-changes TOKEN` recomputes the bundle from the run's own inventory, mappings and locks and compares, so a re-installed Tessl package, a replacement re-resolved elsewhere, a difference that appeared or disappeared, a token from another project, and a token offered to a project with nothing to accept are each refused with an `acceptance-stale` blocker rather than silently honoured. The binding covers the evidence the preview reported, and the same review found the gap in what that was: a rule's `applyTo` clause after the em dash — `docs/** — when editing production credentials` — was parsed, labelled as dropped, and then thrown away, so rewriting the condition that tells an agent when a rule applies left both the label and the token unchanged and let an old token finalize a changed rule. The clause now travels with its own text, in the lossy evidence, in the blocker detail and in the accepted change, so an operator sees which condition is being dropped and the token moves when it changes. Carrying free text into the digest made the old delimiter-joined encoding unsafe — a comma or a NUL inside an operator's prose could reshape an array — so the package evidence is now serialized structurally and covers classification, body digest, activation mode, activation globs, hook event and dropped behaviour, with boundary tests for the array shapes a joined encoding cannot tell apart. Two edits still deliberately do not invalidate a token: rewriting a `description`'s text, which never reaches ACR output and whose disposition the bundle already records, and reordering activation globs, which ACR's model sorts. The token binds evidence, not project identity — there is no project identifier in the bundle, so two projects holding identical evidence share one token, which is the mechanism working as specified rather than a leak; `docs/migration.md` now carries the table of what does and does not invalidate instead of the earlier absolute claim. Acceptance is deliberately not a force switch. It clears one artifact's `effective-diff` and `lossy-artifact` blockers and nothing else, artifact by artifact, and an accepted lossy artifact stops being retained so its Tessl natives retire alongside the rest of the identified output instead of surviving the cutover. An artifact is offered for acceptance only when ACR classified it `migratable` and recorded a digest for it. The first cut excluded only `ambiguous` artifacts, and an independent review reproduced the consequence through the public CLI: retag an installed hook's event to something Tessl never defined, and inventory classifies it `unsupported` with no supported event and no old digest — yet the comparison still reported a plain `body` difference against the understood replacement, acceptance cleared it, and finalization deleted the package. An `unsupported` artifact now raises its own `unsupported-artifact` blocker, consulted by the readiness gate independently of whatever comparison reason its missing evidence happened to produce, so acceptance cannot answer a question about behaviour ACR never understood. `missing-in-acr` is never acceptable either, because an artifact the replacement does not carry has no replacement evidence to review; project ambiguity, uncovered agents, unproven shared links, an unprovable MCP entry, symlink escapes, untracked `tessl.json` or vendor files, pending coexistence state and transaction recovery are outside the mechanism entirely and refuse exactly as before. `--accept-reviewed-changes` requires `--finalize`, and `--non-interactive` — which answers prompts, not questions of intent — never stands in for it. Accepted differences stay in `effectiveDiffs` and are reported again under `acceptedChanges` in both the JSON report and the text output, so a cutover that changed six artifacts never reads as a cutover that changed none. The coexistence and finalization report is stamped `schemaVersion: 3` for the two new fields; the inventory report is graded apart and stays at `2`.
- Cover every public CLI command with a runnable journey, and make an uncovered command fail the suite. The dogfood that produced the previous entries found a universal install failure no test could have caught: nothing exercised a real GitHub source archive through the actual argument parser and production composition, so every command's "coverage" was a mix of service-level tests and empty-project smoke runs that pass whether or not the command does anything. `cmd/acr` now carries one journey table. Each entry runs real argv through the shipped `runWith` composition against a stateful GitHub fixture — releases, commits, tarball and asset redirects, uploads and readback — reached through the production `GitHubClient` at its production hostnames, so the client keeps building, authenticating, redirect-checking and origin-validating exactly the requests it builds against GitHub. Source tarballs carry the leading `pax_global_header` and the group permissions codeload actually serves, so the two failures the dogfood found stay covered by shape rather than by anecdote. `TestCLIJourneyInventory` then holds the completed journeys against `cli.Leaves()`: every one of the 15 executable leaves needs a successful outcome and a refusal, and a journey registers coverage only after asserting a positive result, so a help-only or empty-state test satisfies nothing. The inventory enumerates the dispatch registry `commandFor` accepts rather than the display order, because a first attempt derived it from `commandOrder` and a command added to `commandSpecs` alone was then dispatchable, undocumented and uncovered all at once — the gate now runs against the production surface with exactly that command added and has to name it, alongside `TestCLIJourneyInventoryFailsForTheIntendedCause`, which mutates a synthetic inventory the same ways — a new command, a new subcommand, a removed journey, a leaf that only refuses — and requires a complaint by name for each. Where a journey reasons about elapsed time it holds the clock still: `freshnessapp.NewApplication` takes an optional clock that `publishapp`, `migrateapp` and `runWith` forward untouched, so the throttle journey crosses Window-1s and Window on purpose instead of passing because the suite is fast, and without an option the composition is byte-for-byte the one the binary ships. The publish journey is the whole arc: a real tagged Git repository is published through the boundary and a consumer that has never seen it installs, realizes and checks the release that publishing created, with no post-publication state seeded by hand. Three lanes are recorded separately because they establish different things: the composed in-process stack, a composed subprocess with real descriptors and exit statuses, and the compiled shipped binary for local and offline behavior. What none of them establish — live GitHub conformance, real agent loading, fixture provenance capture — is written down in [`docs/testing.md`](docs/testing.md) rather than implied to be covered, and the two checks that would establish the first two are runnable procedures rather than intentions: [`docs/manual-conformance.md`](docs/manual-conformance.md) carries the read-only binary lifecycle check and the per-agent canary, each with its isolation, the identities to record, the nonempty observations a pass requires and a pass/fail/blocked classification, on a monthly plus contract-change cadence. The canary needed a fixture rather than a paragraph: the first draft sent the operator to the lifecycle check's own scratch consumer, which that check uninstalls twice and then deletes and which only ever selected two of the three adapters, so following the stated order left nothing installed to observe. The canary now has its own consumer and its own local package — [`docs/canary-package/`](docs/canary-package) carries an always-on rule, a rule scoped to `docs/**`, a discoverable skill and a session-start hook that appends one marker line to a scratch log and does nothing else, vendored through `acr migrate tessl` so no publication and no network are involved. The phases are numbered with the observations before the teardown, each observation names how it is captured and what makes it a failure, and `cmd/acr/canary_test.go` executes every phase except starting the agents, so what stays manual is the observation rather than the setup. The first version of those observations could still pass without the thing they describe ever happening: every agent read one shared marker log, so a runtime whose hook never ran inherited the first runtime's line, and a rule row was satisfied by an agent that searched the fixture and quoted the sentinel back. Each agent now gets its own absolute `ACR_CANARY_LOG`, proven absent before the session and holding a new marker after it, with a reset-and-increment form for runtimes that cannot pass an environment variable through to a hook — a marker that was already there, another agent's line, a registration, or a hook run by hand never counts. That fallback could not run at all on the consumer it was written for: it copied and truncated a log that only exists once a hook has appended to it, so on a freshly prepared consumer both the copy and the truncation failed on the missing `.acr-canary/` parent and the first observation had nowhere to start. It now creates the parent, archives a prior log only when there is one and records the absence when there is not, and initializes the log itself; the steps are chained so an archive that fails stops the sequence rather than truncating evidence it could not preserve. It also says where the agent has to be started, because with `ACR_CANARY_LOG` unset the hook writes to a relative path that resolves against whatever working directory the runtime hands it. The rule rows require clean sessions and tool-free first turns, prompts that name neither a sentinel value nor a fixture path, and whatever tool or attachment trace the runtime already provides; an answer that came from a file search is invalid rather than a pass, and a runtime that cannot show the difference is blocked rather than passing. Skill discovery is established from a native listing before the skill is invoked, and the scoped-rule row belongs to Cursor alone, because ACR's Cursor adapter is the only one that realizes path scoping into its own `.mdc` while its Claude Code and Codex adapters render the same rule into the always-on host, which makes the row a statement about what ACR wrote rather than about what those runtimes can do. [`cmd/acr/fixture-provenance.md`](cmd/acr/fixture-provenance.md) sits beside the builders and separates what was observed from a real GitHub tarball — endpoint, observation date, capture digest, PAX header, root, mode sets, ordered entries — from the response bodies written from the API contract, names the capture fields nobody kept, distinguishes the capture digest from the published asset digest and the canonical content hash, and states the same refresh cadence and its reviewed structural diff. Three of the tests the review round then read were asserting spelling rather than outcome, and each had a way to be wrong in both directions. The composed clock's default was checked by `reflect.Pointer` identity against `time.Now`, so wrapping the default in a closure that reads the same source broke a test nothing observable had changed; it is now held to what a live clock does — a non-zero reading that never runs backwards — which a wrapper leaves passing while a default that stopped reading a real clock still fails. The canary hook's isolation rested on a word-boundary regexp over program names plus a substring search for `$HOME` and `http://`, which a comment mentioning `curl` fails and a program reached through a variable passes; the hook now runs in a sandbox with every program it would need shadowed on `PATH` by a recording stub, a home of its own and a log path carrying a space, so an external call, a write outside the log and a split path each fail by name, and a second test covers the log it cannot write — a stderr diagnostic naming the marker, nothing created, and an exit that leaves the agent session running. The hook itself gained the entry-point guard that makes importing it possible without recording a dispatch that never happened, and the compiled binary the three meta lanes share gained a `TestMain` that removes its build directory once every consumer has finished, which no per-test cleanup could do and which a failed build previously leaked outright. The canary skill also picks up the execution-mode preamble and the flat numbered step every skill it stands in for carries, and the rationale sentences on the always-on rule and the skill move here, off a surface that is loaded into every session. The credential-order fakes were passing over a failure of their own: the scratch `PATH` holds nothing but the fakes, so the `cat` the git fake used to consume the credential request was not there, and without strict mode the fake carried on past the missing command and printed the password anyway — the order was proven while the request was never read. Each fake now runs bash at a resolved absolute path under `set -euo pipefail`, consumes the request with shell builtins, and answers only a request that names the host, so dropping the host from what discovery sends fails the case.
- Answer `acr init` over a real pseudo-terminal in the journeys. The interactive path was previously proven only by the probe that decides it and by the refusals every non-terminal shape takes; a typed selection now travels through an actual terminal into `agents.yaml`, and an unparsable answer re-prompts instead of choosing something nobody asked for.
- Prove the credential discovery order end to end. `GH_TOKEN` before `GITHUB_TOKEN` before `gh auth token` before `git credential fill` was only ever exercised by injecting a token provider, which proves nothing about the order the commands are tried in or about the `GIT_TERMINAL_PROMPT=0` that keeps a helper from blocking. The order is now driven through real executables on a scratch `PATH`.

### Changed

- Bump the `claude-code` adapter to `1.0.2`. Its rendered `.claude/settings.json` hook command changed with the quoting fix above, and [`docs/adapters.md`](docs/adapters.md) makes an adapter's own output change a `Descriptor.Version` bump while the boundary stays `1`. `codex` and `cursor` are unchanged at `1.0.1`.
- Render the coordinator-owned `.agents/skills` surface through the same package-wide reference API the per-agent adapters use. `SharedSkillIntents` called the two-root `RebaseSkillReferences`, which carries no identity and so rewrites neither a cross-skill path nor the legacy `.tessl/plugins/<identity>/` form; the shared surface therefore diverged from [`docs/realization.md`](docs/realization.md)'s promise that it receives "the same content and modes a per-agent skill tree receives". It now resolves `PackageSkillReferences` per package and applies `RebasePackageReferences`, the way `claude-code`, `codex` and `cursor` already do.
- Send both republication branches through the version step. The same-name branch skipped from the identity edit straight to committing, and conversion regenerates `version` from the Tessl manifest — so following it put the already-published version back into the manifest and `git tag` refused with `tag 'v<version>' already exists`. Only the identity edit is branch-specific now; the version step is not. `TestSameNameRepublishRoundtrip` publishes an old version, republishes over it, and requires the earlier tag and release to survive while the new version installs and its skill and rule commands execute.
- Document the producer sequence that publishes a converted package under a different repository name. The previous instructions told a producer to migrate with the publication repository's URL, which exits 1 with `invalid_source`: `source.repository` must equal `https://github.com/` + `name`, so conversion cannot be handed an identity the plugin does not carry. Conversion runs under the original Tessl identity — which is what lets it record `source.tesslIdentity` — and the producer then edits `name`, `source.repository` and `version` in the manifest it has not published yet, leaving the recorded identity alone. `cmd/acr/producer_rename_test.go` runs the sequence, including that refusal, through the production commands against a local fake remote, then installs the publication and executes the six skill and three rule commands the realized content instructs an agent to run.
- Control the credential-discovery deadline in tests. `go test -race ./...` failed in `TestDiscoverGitHubTokenPrefersEnvironmentThenCommands/git_credential_fill_is_the_last_resort`, which returned no token where it wanted `from-git`; the subtest took 10.07 seconds against 0.52 in a focused run, and the only timer in that path is `commandToken`'s five-second per-command deadline. That points at the budget cutting the commands off, but the run was never traced and no kill was observed, so it stays an inference from timings — which is exactly why the repair does not rest on settling it. The test was asserting the order the sources are tried in, and racing a wall clock to do it. The native branch introduced `discoverGitHubTokenWithin` with a per-command budget; main integration retains the already-merged `credentialProbeTimeout` / `credentialProbeContext` seam instead. The order and caller-cancellation tests set that seam to zero, while the shipped binary keeps its five-second bound. Both branches’ credential coverage is retained, including the native blocked-helper checks. The budget and the caller's context keep their own tests, on a fixture that genuinely blocks: it consumes the credential request with shell builtins, records that it reached the blocked state, and opens a FIFO nobody ever writes to. The first version of that fixture called `sleep` on a PATH holding nothing but the fakes, so it exited 127 before blocking and its empty output passed the cancellation assertion with no cancellation at all — running those bytes with no deadline is what exposed it, and running the new ones the same way leaves a process sitting in the kernel. The marker file is what separates the two: a fixture that never ran cannot pass as one that was abandoned.
- Bump the `claude-code`, `codex` and `cursor` adapters to `1.0.1`. Their skill output changed with the reference-rebasing fix above, and [`docs/adapters.md`](docs/adapters.md) makes an adapter's own output change a `Descriptor.Version` bump rather than a boundary bump; the adapter/coordinator API those implementations are compiled against is unchanged, so the boundary stays `1`. The recorded `adapterVersion` in every ledger entry follows, which is metadata nothing compares — a consumer realized against `1.0.0` re-realizes without a drift refusal.
- Publish one CycloneDX SBOM per CLI target instead of a single linux/amd64 document, closing issue #41. Release assets are now the four archives, `acr-<os>-<arch>.cdx.json` for each target, checksums, and the Sigstore bundle. Validation rejects a document whose `cdx:gomod:build:env:GOOS` / `GOARCH` properties or purl qualifiers do not match the named target, so a renamed linux SBOM cannot stand in for darwin or arm64. Package releases stay on the existing three-asset contract. Two rounds of review found the proof of that workflow asserting its source text rather than its behaviour, wrong in both directions each time. Requiring the literal `for target in …` loop and the `acr-${target}.cdx.json` filename passed on a commented-out run body and failed on a reordered target list, so the step became one the tests execute. The replacement moved the same defect inside itself: a hand-written jq double authenticated the exact program text, so `.metadata.component += {"name":"acr","version":$version}` — which real jq turns into a byte-identical document — failed with `unexpected jq expression`, and `--goos "${goos}"` / `--goarch "${goarch}"` in the contract list broke on a consistent variable rename while being the only thing that noticed the whole `verify-sbom` call being deleted. Replacing those with helpers that derived the loop and the variables from the script instead of naming them only shrank the surface: the derivation still had to be spelled `${target%-*}`, so switching to the equivalent `${target%%-*}` reddened the suite before anything ran, and the `go` double dropped the package argument entirely, so pointing the step at `./internal/no-such-package` — which real Go cannot resolve — left every test green. The proof no longer reads the production script at all. One test runs the workflow step and holds it to what it produced: four reproducible per-target documents in `release-assets`, each valid for its own target and rejected as the other three, the graph intact, one pinned generator install, and four validations identified by the paths production `verify-sbom` accepted — which it grants only when the document it was handed is the one named for the target it was told to check. Everything that proves those assertions discriminate runs against authored fixture scripts instead, so a fixture may spell things explicitly without that becoming a claim about how the workflow must be written; one of them names all four targets with no loop and no parameter expansion at all. jq and the Go toolchain are real, and the two remaining doubles refuse what they cannot honour rather than ignoring it: the generator rejects an unknown flag, refuses to run without `-json` or a loadable `-main`, and emits the `serialNumber` and `metadata.timestamp` that `-noserial` and `-notimestamp` suppress — so dropping either flag from a step named "Generate deterministic CycloneDX SBOMs" now fails instead of passing green. Eighteen breaking changes to the step are caught, and five behaviour-preserving ones — reordering the targets, renaming the variables, either equivalent jq program, the longest-match expansion, and deriving the target with `cut` instead of expansion at all — all pass. `jq` joins Git as a stated prerequisite in `CONTRIBUTING.md`, and the tests name it when it is missing instead of surfacing a bare `command not found`. A further round found the last two places the proof still rested on something other than behaviour. Requiring the literal `cdx:gomod:build:env:GOOS` and `GOARCH` strings anywhere in the workflow was the same defect once more, and both directions were reproduced against the previous tree: it reddened on the behaviour-equivalent `("cdx:gomod:build:env:" + "GOOS")`, and it stayed green on a workflow whose check had become `jq -e 'true'` with the two names left behind in a comment. The verify job's `Verify, extract, and execute the target asset` step is executed instead. Each of the four targets is verified against each of the four documents, accepting its own and rejecting the other three, and every accepted run is one that authenticated the checksums, unpacked the archive and ran the binary inside it. A table then varies one thing at a time — a `GOOS` or `GOARCH` property naming another platform or absent entirely, an identity never rewritten, another release's version, an archive that does not match the manifest, a binary reporting another version or commit — and requires the executed step to refuse it by name. Three behaviour-preserving rewrites of that step pass, the concatenated property names among them, and eight breaking ones fail, including the commented-out check. The archived executable is a labelled fixture reporting fixed values, because a cross-compiled binary only runs on a runner of its own platform; the hosted verify matrix remains what exercises the real one. `shasum` and `sha256sum` are supplied as one controlled SHA-256 checker, so the checksum gate reaches the same verdict on a host that ships neither. The other place was tool isolation: `no-generator-install` required `cyclonedx-gomod: command not found`, which only holds for a developer who never installed the generator, and with a real v1.12.0 first on `PATH` that unchanged fixture found it, ran it, and failed on the diagnostic it got back. The lookup now resolves inside the run's own install directory every time — a named executable occupies that path until the simulated install replaces it — so an installed generator cannot change an outcome. A control runs the production step and the no-install fixture twice each, once with an executable named `cyclonedx-gomod` on `PATH` behind that directory, and requires the same verdict from both with the ambient executable never called. A further round found three assertions still reading source text and one generated script opening without strict mode. Requiring the pinned `github.com/CycloneDX/cyclonedx-gomod/cmd/cyclonedx-gomod@v1.12.0` string anywhere in the workflow, banning the legacy `release-assets/acr.cdx.json` filename by substring, and requiring the exact phrase `ten public CLI release assets` in `docs/install.md` were each wrong in both directions, and both directions were reproduced against the previous tree. The pin assertion reddened on an install assembled from two shell variables and stayed green on an unpinned `@v1.11.0` install with the pinned string left behind in a comment. The filename ban reddened on a comment naming the old asset and stayed green on `${legacy_stem}.cdx.json`, which emits exactly the one document for four platforms that issue #41 exists to prevent. The phrase assertion reddened on `10` written in place of `ten`. All three are gone, and what replaces them was already running: `TestReleaseWorkflowGeneratesFourTargetSBOMs` executes the workflow's own generation step, `checkPinnedGeneratorInstalled` requires the one recorded install while the `go` double refuses any specification but the pin, and `checkGeneratedReleaseSBOMs` requires the four per-target documents and nothing else in `release-assets` — so a drifted pin, an unpinned install hidden behind a comment and a legacy name built from variables are each caught by what the step produced, while the two rewrites that change nothing observable pass. `docs/install.md` keeps the ten public asset names themselves — the four archives, the four SBOMs, `checksums.txt` and `checksums.txt.sigstore.json` — so renaming one still fails while rewording the sentence around them does not. The executable that occupies the generator's install path until the step installs the generator there now opens with `set -euo pipefail` after its shebang, keeping the diagnostic it prints and its explicit `exit 127`.
- Make the executable command surface enumerable. The accepted subcommands of `freshness` and `migrate` were literals inside parser branches, so nothing outside the parser could list what a user is able to run, and the meta commands were a hard-coded switch plus two hard-coded help lines. Both are descriptors now, `cli.Leaves()` returns the 15 executable leaves the parser dispatches on and help renders, and the acceptance suite enforces coverage against that rather than against a hand-maintained list or a Go AST reader that any internal refactor would break. `NewGitHubClient` takes options so a test can supply an HTTP client while keeping the production API host, upload host and trusted redirect origins; without options it is the client the binary already shipped.
- Make the fake GitHub remote observable, closing issue #67. `PublishRelease` mutated a range-loop copy, so a published release stayed a draft in every stored representation; `DeleteRelease` logged the identifier and left the release visible to later lookups; `UploadAsset` never associated the asset with its release. A publisher-to-consumer journey through that double could only pass by seeding the outcome it was meant to prove. The live release index is now what every lookup reads and every publication method mutates, the creation and deletion slices are the event logs they were named for, and reads hand out copies so a caller editing a returned asset slice cannot corrupt storage. Repairing the mutations was not enough on its own: the `Latest` and `Releases` seeds still answered before the live index, so a release deleted through the publication API kept answering the consumer views seeded with it and a release published through the double stayed hidden behind an older `Latest` seed — one `Remote` could call the same release absent for a publisher and present for a consumer. Every lookup now resolves live index first, seed second and tombstone never; a publication advances what `LatestRelease` returns; a seeded release can be uploaded to and published, which promotes it into the live index; and the seeds a caller wrote stay where they were put. Unifying the views moved the remaining defect into identifier allocation, because latest selection and deletion then keyed on identifiers the counters handed out from fixed floors regardless of what a caller had seeded: a seeded release at 1001 collided with the next generated one, so deleting the new release tombstoned the seeded one; a seed at 5000 hid every later publication behind it; and an upload reused a seeded asset id, rewriting the bytes of an asset nobody uploaded to. Allocation now advances past the highest identifier visible anywhere — the live index, every seed, the assets those releases carry, the creation log and the tombstones, so a deleted identifier stays reserved — and no caller adjusts a counter and no fixture lowers an id to make a test pass.
- Stop hashing Git's own storage in documented-command snapshots, closing issue #76. Background maintenance wrote `.git/objects/maintenance.lock` while a documented command ran, and the before/after comparison failed for a reason the command under test had nothing to do with. The shared snapshot skips Git internals and records what the promises are made of instead — permission bits, symlink targets and directories alongside content digests — plus `.git/info/exclude`, which ACR owns a generated block in. Excluding the storage also hid the index and the refs, so a command could stage a mode change or move HEAD and every composed journey would still call the project unchanged; the snapshot now records what Git answers rather than what Git stores — the commit HEAD names, the branch, every index entry with its mode and blob, the working-tree status, and the exclude file resolved through Git so a linked worktree's common-directory copy is covered too. The probe stages a mode change and moves HEAD without touching a product byte, and both have to fail the snapshot while maintenance lock churn still must not.
- Supply the repository metadata stage 0 of the migration guide actually needs, for issues #83 and #84. Following the stage verbatim on a real Tessl plugin, the first prescribed `acr migrate tessl-plugin --dry-run` refused at `required`, because real manifests have no `repository` field and the guide never said to supply one. The stage now names the prerequisite up front and demonstrates `--repository` on both the rehearsal and the apply. It also told the operator to review the proposed `agent-plugin.yaml` before applying, which neither the text nor the JSON dry run emits — those bytes exist only once the conversion has run for real — so the review moved to where the file exists rather than growing a preview flag the tool does not need. The `producer` documentation fixture drops its repository field so the executable harness runs the walkthrough against the shape the guide claims to describe.

### Fixed

- Correct clean producer conversion to normalize `.git` clone URLs, refuse unsupported owned references across authored text, and use scoped receipts that survive ordinary Git clones without binding unrelated repository files.

- Widen the `project_state_conflict` row in [`docs/troubleshooting.md`](docs/troubleshooting.md) to the refusals `compatibleProjectState` actually makes, closing issue #111. The row named two triggers — different agents, or a different requested version — while the code also refuses a declaration that was dropped and a lock that was dropped or re-resolved to a different release, tag, commit, package version, content hash or hold. An operator hitting a lock-resolution conflict read the row, recognized neither trigger, and mis-triaged it. The exit code, error code, remedy and the sentence about unmapped packages being retained are unchanged.

- Preserve ACR dependencies a Tessl mapping does not name, closing issue #107. `acr migrate tessl` refused an entire consumer with `project_state_conflict` — "existing agents.yaml dependencies disagree with the Tessl mapping" — whenever the project already had an ACR package installed, which is precisely the gradual migration coexistence mode exists for. The reproduction that found it ran the released 0.1.5 binary over a real consumer holding Tessl `tessl-labs/good-oss-citizen@1.1.12` beside an unrelated ACR `github:jbaruch/ffa-acr-dogfood@v0.9.39` for Claude, Codex and Cursor: `--vendor-unmapped --dry-run` exited 1 having compared nothing, because the two packages have nothing to do with each other. The cause was on both sides of one comparison. `resolveState` built the desired state out of the mapped packages alone, so an unrelated declaration and its lock were simply absent from it; `compatibleProjectState` then demanded the existing and desired dependency sets be equal, which no first migration of a non-empty project can satisfy. `resolveState` now carries every declaration and lock the mapping does not name straight through — requested policy, hold, extension fields, release, commit, package version and content hash byte for byte — and stamps the state files at no lower schema version than the retained state needs, so a retained vendor source or hold is still expressible in the file the run writes. Retaining the entries is also what keeps the retained packages realized: the realizer plans against the desired state, and a state missing the unrelated package would have removed its native output along with its lock. The one entry that is not retained is a vendored source the same run supersedes with an upstream one, whose tree that run removes. `compatibleProjectState` now asks whether the existing state survives into the desired one rather than whether the two are identical, which keeps every deliberate refusal: a mapping that re-requests or re-resolves a package the project already declares, a dropped declaration or lock, a differing agent selection, an identity collision, and #101's diagnostic-only case for genuinely conflicting state. Regressions run the public command surface on both reported paths — an explicit pinned GitHub mapping and `--vendor-unmapped` — over a project whose retained state carries a pin, an extension field and a `latest` declaration parked behind a rollback hold, and hold the rehearsal to writing nothing, the apply to adding only the mapped packages, the repeat to inertness, and the retained package's realized block to its exact bytes. A full journey through the shipped composition installs and realizes an unrelated package, migrates a Tessl installation beside it, and finalizes: `tessl.json` goes, and the unrelated declaration, lock, realized bytes and executable modes are unchanged through every step.

- Rebase bundled references inside emphasized Markdown code spans (#104), including the published blog-writer tone guide line that remained at its package-root path after migration. Ordinary `*`, `**`, `***` and underscore emphasis before a code span preserves the reference start, while filenames, URLs, quoted argument interiors and foreign Tessl identities retain their bytes. Normal realization replaces previously generated content. The combined unpublished #103/#104 candidate uses Claude adapter `1.0.3`, Codex and Cursor `1.0.2`, and shared-skill compiler `3`; the adapter boundary remains `1`.

- Fix Claude command hooks failing to spawn when arguments are present (#103). The Claude adapter now uses native exec form consistently: an unquoted executable path and an explicit `args` array, including an empty array for zero arguments. Literal argv boundaries survive spaces and shell-special characters. Adapter version 1.0.3 reconciles existing 0.1.4 settings through ordinary realize/check while preserving user and Tessl content. Process regressions exercise direct spawning and the prior quoted-path failure.

- Quote the whole Claude hook command, expansion included, closing issue #100. `claudeProjectCommand` quoted only when the *relative* hook target carried a shell-sensitive byte, so an ordinary filename rendered a bare `${CLAUDE_PROJECT_DIR}/.claude/hooks/…`; a project directory named `project with spaces` then split that path and the hook never ran, which is exactly what a live Claude Code session reported (`/bin/sh: /private/tmp/…/consumer: No such file or directory`). The relative target is the only half the adapter can see, and it is never the half that decides. The command is now always `"${CLAUDE_PROJECT_DIR}/<escaped target>"`, which still expands and no longer word-splits. The regression is execution-based rather than a string comparison: both an ordinary filename and one containing a space are realized into a project root whose path carries a space, and the generated command is run through a real shell from a different working directory, with the hook's arguments as its argument vector. Against the previous renderer the ordinary-filename case exits 127. A second regression realizes over the materialization an older ACR left — the bare command, recorded in the ledger as owned — and holds the upgrade to replacing that element in place, with no duplicate handler, no leftover unquoted command, and the foreign settings around it intact.
- Stop the credential discovery tests racing the probe deadline they run under. Every probe in `commandToken` was bounded by a hardcoded five seconds, and the precedence fixtures answer from real executables on a scratch `PATH`, so each case only passed when a forked shell started inside that window. Under the full `-race` suite one did not: `gh` was killed, discovery fell through to the `git` fake, and `gh follows the environment` asserted `unreachable` 8.18 seconds in. Later reruns passed, which is what a load-dependent test does rather than evidence that it is sound. Expiring only the `gh` probe on the base tree produces that exact failure line with no timing involved, which establishes the causal path and is consistent with what the original run recorded; which shell the operating system stalled, and when, is not recoverable from those logs and is not claimed. The bound is named `defaultCredentialProbeTimeout` and reached through `credentialProbeContext`, which now takes the instant its deadline is measured from instead of reading the clock itself: the behaviour fixtures run with no deadline to lose, the expiration cases run with one already gone and start no process at all, and the shipped bound is asserted as exact arithmetic against a fixed instant with the five seconds written independently of the production constant, so widening it to an hour fails immediately rather than after an hour. What the shipped binary does is unchanged.

- Hold credential discovery to the commands it actually runs. The fixtures proved the order and little else: either fake answered whatever argv it was handed, the credential request was accepted on a substring, prompting was checked against a parent that already had it disabled, and a missing, unexecutable or silently empty `gh` was never tried at all. Each scratch command now refuses anything but `gh auth token` and `git credential fill`, the credential request is held to its exact bytes rather than to fields recognized inside it — read whole and compared whole, because folding each newline into a separator makes a line break indistinguishable from that separator and drops an unterminated final field, which let a single flattened line with no host field and no terminating blank line answer as though it were the real request — and the parent exports `GIT_TERMINAL_PROMPT=1` so production's own override is what keeps the helper from blocking. The probes append to a trace, so order and the absence of a lower-priority call are outcomes rather than inferences, and the table covers a missing binary, an unexecutable one, a successful but empty answer, a failing command whose output must be ignored, an empty or absent password, and the anonymous client each of those ends at. The token cache is established the same way: the shipped client discovers through the real commands once, every credential it could read then changes, and both the authenticated and the anonymous first answer have to survive the next request with no second probe in the trace.

- Keep a quoted argument whole through the quotes and escapes inside it, and rebase a quoted assignment value, for issue #92. The enclosing-argument model the previous round introduced was right and its bookkeeping was not: the opaque range was a single pair of offsets that any inner quote overwrote, so `"archive 'nested' skills/advocate/check.sh"` — one ordinary argument that opens with `archive` — had its interior replaced on all three adapters, as did the inverse nesting, and `IndexByte` took the escaped quote of `"archive \" skills/advocate/check.sh"` for the terminator and ended the argument early. A quote inside an already-open argument now opens nothing, and a double-quoted argument's terminator search honours the backslash escape the shell honours; a single-quoted one still has no escape, which is also the shell's rule. Separately, `HELPER="skills/advocate/check.sh"` and `--file="skills/advocate/check.sh"` combine two promised supported forms — the assignment prefix and the quoted value — and rebased as neither: the quote after the prefix was not at a token start, so it opened no argument, the value stayed at the package-root path, and executing the realized assignment exited 127 on every adapter. An assignment prefix is a place an argument may start now, so the quoted spelling names the helper its unquoted spelling names. One piece of that bookkeeping was still conflating two questions: the first position inside an argument may begin a reference, and the scanner read that eligibility as the position being outside the argument. A quote written there therefore opened a second argument, and when that one closed the rest of the first argument was scanned as ordinary text and rebased — `"'archive' skills/advocate/check.sh"` acquired a native path on all three adapters, as did the inverse quoting, the same shape inside `LABEL="…"`, and an argument opening with a literal single-quoted path. Twelve realizations were corrupted and, under `sh`, all twelve argument values differed from the ones the package wrote while every command still exited 0. Being inside an argument and being able to begin a reference are separate states now: the interior runs from the argument's first byte, so a quote there opens nothing, and that first byte alone stays eligible, so a supported reference written at it still rebases. `TestRealizedQuotedAssignmentExecutesTheHelper` runs the realized assignments through `sh` and holds their output to the helper's contract on all three adapters; `TestRealizedArgumentsKeepTheValuesAShellReads` reads the fixture's complete arguments back through a shell on all three and requires the preserved ones to carry the value their package bytes gave them and the rebased one to carry the installed path; and the boundary table carries the nested, escaped, opaque-value and first-position negatives beside the first-position positives. One boundary was still open at the other end of the same argument: the scanner cleared its interior as soon as the index reached the closing quote, so an argument whose last interior byte was whitespace left the scanner fresh and its own terminator took the opening branch. The terminator then paired with the next quote in the document and everything between them was scanned as argument interior — `sh skills/advocate/check.sh "label "` followed by `sh skills/advocate/check.sh "last"` realized one installed helper and one path that does not exist from the project directory, so the realized pair exited 127 on all three adapters while a same-line variant lost a helper and still exited 0, and `"archive "skills/advocate/check.sh` — one shell word whose interior space is quoted — acquired a native path in the value a command reads. A known terminator is consumed as the terminator now, and the word carrying it continues past it, so an opaque concatenation keeps its bytes while `""skills/advocate/check.sh`, whose empty argument contributes nothing to that word, is still the path alone and still rebases. `TestRealizedClosingBoundaryCommandsExecute` realizes the fixture's composed block on all three adapters, runs it from the project directory and holds its exit status, stdout and stderr to the same block run from the package root, so a helper that did not run and an argument that arrived with different bytes each fail it.
- Let a Markdown link label wrap onto a second line, for issue #92. The round-3 scanner cleared the label stack at every newline, on the stated ground that a Markdown link does not span lines. [CommonMark](https://spec.commonmark.org/0.31.2/#links) says otherwise: link text is inline content, and the only thing it cannot contain is a blank line. `[helper\nlabel](skills/advocate/check.sh)` therefore kept a destination pointing at a source path that does not exist in a realized tree. The stack resets at a blank line instead, which is the actual boundary, and the guard that matters is unchanged — `](` still opens a destination only where a `[` opened at a token start, so `archive](skills/…)`, `https://example.test/a](skills/…)` and a label opened inside a word all keep their bytes, and a label left open across a blank line no longer reaches a later `](`.
- Read a declared `source.tesslIdentity` through the aliases and merge keys the decoder already resolves. Reading presence from the document tree fixed the empty-and-null divergence below and introduced a narrower one in both directions, because the walk looked only at keys written directly into the `source` mapping: a `<<`-merged `tesslIdentity: null` or `""` passed `manifest.Load` and failed the shipped schema, while a valid `*alias` — an identity anchored on `name` and referenced by `source.tesslIdentity` — passed the schema and failed `Load`, which required a `ScalarNode` and got an `AliasNode`. Both are forms the strict decoder accepts, so refusing them to manufacture parity would have narrowed the manifest dialect rather than aligned the two surfaces. The walk resolves aliases and merge keys with the merge specification's precedence — a directly written key beats every merged one, the earliest merged mapping beats the later ones — and a quoted `"<<"` stays an ordinary key the strict decode rejects as unknown. The same conflation reached the keys: an `AliasNode`'s own `Value` is the anchor name rather than the name the document writes, so `*identityKey: null` — with `identityKey` anchored on the scalar `tesslIdentity` — was a key the walk could not see while the strict decoder read it as the field it names. That hid a written-out null twice over, since the merged value the walk found in its place then won a precedence contest the explicit key wins, and the same blindness applied to an aliased `source` key. Keys resolve exactly as values do now, at both lookups. Unknown-key rejection, omission compatibility and migration output are unchanged; `TestDeclaredIdentityParity` runs 33 authored documents through both surfaces with no struct in between, `TestDeclaredIdentityParityThroughAnAliasedSourceKey` covers the other lookup, and `TestStrictDecodeRejectsBeforeTheIdentityWalk` pins the ordering the walk depends on — a duplicate key, an unresolved alias and two self-merging documents are each refused by the strict decode, by the diagnostic it reports, before the walk runs. That test also corrects the claim the alias bound carried: cyclic documents do parse into nodes, and it is the decode ordering rather than the parser that keeps them away from the walk. The same conflation had one layer left: a resolved node's `Value` is still the spelling the document writes, while the pinned decoder reads a tagged scalar as what it encodes, mapping keys included. `!!binary dGVzc2xJZGVudGl0eQ==` is the field `tesslIdentity`, so an encoded key carrying an explicit null or empty string passed `Load` and failed the schema, an encoded key lost a precedence contest to a merged value it wins, an encoded `source` key hid the identity written under it, and a valid `!!binary` identity value was refused by `Load` while both the strict decoder and the schema accepted it — six of twelve raw documents disagreeing across the two surfaces, every one of them accepted by the strict typed decode. Keys and the identity value are read as the decoder reads them now, while authored presence and null still come from the document tree; the twelve documents are committed across `TestDeclaredIdentityParity` and `TestDeclaredIdentityParityThroughAnEncodedSourceKey`, which run raw bytes through `Load` and the compiled schema with no struct in between.
- Decide a reference's start from the structure around it, for issue #92. Two rounds of punctuation rules kept moving the same defect to the next character: with a bracket-in-word test in place, `archive](skills/advocate/scripts/check.sh)` and `https://example.test/a'skills/advocate/scripts/check.sh` still took an interior native prefix on all three adapters — 30 corrupted observations across five shapes, two skills and three adapters in an independent probe. One byte of lookbehind cannot answer the question, so the scanner now carries the enclosing structure and answers it directly. It tracks the word it is reading, so a quote or a bracket inside a run of non-whitespace bytes is part of that word rather than an opener; an argument container, so a `'` or `"` that opens at a token start and has a closing partner makes one argument whose interior whitespace separates nothing — while a quote with no partner opens nothing at all, so an odd quote cannot swallow a file, and a backtick stays a Markdown code span where a backquoted command's arguments each still begin a reference; and a label stack, so `](` opens a destination only where a `[` actually opened. That round reset the stack at each newline on the belief that a Markdown link cannot span lines, which is not what CommonMark says; the entry below carries the correction. Every previously working positive is retained, including the 24 extracted command executions and the 12 link, parenthesized and assignment forms.
- Refuse a written-out empty `source.tesslIdentity`. `tesslIdentity: ""` and `tesslIdentity: null` passed the loader and failed the shipped schema, because both decode to the same empty Go string that omission produces — and the parity test could not see it, since assigning `""` to the struct and marshalling it erased the field through `omitempty` and tested omission twice. Presence is now read from the document tree, before decoding flattens it: written out, the field must be a package identity; omitted, it is absent. The parity suite runs authored document bytes through `manifest.Load` and the compiled schema without a struct in between, over an explicit empty string, an explicit null, an empty value with no scalar, a sequence, valid and invalid identities and an unknown sibling key. Real migration output keeps validating on both surfaces.
- Keep a bracket inside a word from starting a reference, for issue #92. Cutting content at every bracketing byte moved the previous round's interior-rewrite bug to a different punctuation boundary: `https://example.test/(skills/advocate/scripts/check.sh)` became `https://example.test/(.claude/skills/acr__…/scripts/check.sh)` on all three adapters, and so did a bracketed URL query and the ordinary filename `archive(skills/advocate/scripts/check.sh)` — 18 corruptions across two skills and three adapters in an independent probe. The delimiter set is gone. A reference now begins a token, and a token begins at the start of the content, after whitespace, after one of the three quote characters, after a Markdown destination's `](`, and after an opening bracket that itself begins a token. That last clause is the whole distinction: `[helper](skills/…)` and a parenthesized `(skills/…)` in prose still rebase, while a bracket embedded in a word continues that word, so the URL, the query value and the filename keep their bytes. The assignment prefix that keeps `--file=skills/…` working reads a bounded name run rather than searching for an `=`, so a URL's `?next=` cannot qualify either.
- Resolve an import of an instruction host ACR is about to write, for issue #93. Deselecting every Markdown adapter removes the ACR-created `AGENTS.md`, and a user's `@AGENTS.md` in `CLAUDE.md` survives it; reselecting them refused with `unresolved_include: CLAUDE.md:2: include "AGENTS.md" does not resolve to a regular file`, and because the host has to be discovered before it can be regenerated, ordinary reselection could not repair it — the user's only way back to their agents was to delete their own import. Realization now discovers the include graph through `DiscoverSelectedIncludeGraph`, where an include naming a host this run writes resolves even while the file is absent. Nothing else is relaxed: an include naming a file ACR does not write is still a refusal, and the plain snapshot reading is unchanged for migration. The whole cycle — all three adapters, Claude, Codex, Cursor, all three again — now realizes, settles and executes the helper its rule names at every step, with the foreign prose intact throughout.
- Restore the two-root `RebaseSkillReferences` signature. Changing an exported function of `internal/adapter` is a package compatibility break that `CurrentBoundaryVersion` is the constant for, and the boundary stayed at 1. The original call compiles and behaves as documented again; the whole-package form that carries cross-skill roots and an evidenced Tessl identity — which two strings cannot express — is `RebasePackageReferences`. Adapter output version `1.0.1` is unaffected.
- Accept `source.tesslIdentity` in the shipped JSON Schema. Every producer conversion emits the field and the Go loader reads it, while `schemas/agent-plugin.schema.json` kept `source` closed to `repository` alone — so real migration output was invalid under the schema the repository advertises for that same contract. `source` stays closed; it now declares the optional field against the same package-name pattern `name` uses, and the parity suite holds the loader and the schema to one answer for an omitted, matching, differing, malformed and unknown key alike.
- Rebase a reference only when the package can prove it owns the tree, for issue #92. The first fix accepted any two path segments after `.tessl/plugins/`, so a package declaring `skills/advocate` rewrote another package's `.tessl/plugins/other/plugin/skills/advocate/scripts/check.sh` into its own directory: an independent probe extracted that command from realized content and it ran this package's helper instead of the one it named, on all three adapters. The identity is the only evidence separating "my own installed tree, before ACR owned it" from "somebody else's tree that happens to expose the same skill path", and it cannot be recovered from the ACR name — `source.repository` binds that name to the GitHub repository, so a package republished under a different repository name loses it entirely. Migration is where the identity is still known, so migration records it: `acr migrate tessl-plugin` writes the Tessl plugin name it converted into the new optional `source.tesslIdentity`, `acr migrate tessl --vendor-unmapped` writes the `<workspace>/<package>` the tree was installed under, and the adapter rebases a legacy reference only for a recorded identity or the package's own name. A package published before the field existed keeps its package-root references working and preserves its legacy ones rather than guessing; [`docs/migration-producer.md`](docs/migration-producer.md) carries the convert-and-republish path that restores them, which is what the FFA dogfood package needs — its 27 legacy references carry the `jbaruch/frequent-flyer-advocate` identity its files were written against while the published copy is named `jbaruch/ffa-acr-dogfood`, and nothing in those bytes says the two are the same package. Consumers reject an unknown manifest key, so the CLI release ships before any package carrying the field.
- Recognize a reference as a whole token rather than by the byte in front of it, for issue #92. Deciding on one preceding byte rewrote a URL query value (`https://example.test/?next=skills/...`), a filename with a punctuation or non-ASCII prefix (`archive#skills/...`, `caféskills/...`), and — through an identity reader that took the next two slashes whatever lay between them — a purported workspace containing whitespace, prose spanning two lines, and the closing quote, newline and opening quote between two adjacent Python literals in the published FFA package's own `tests/test_trackers.py`. The previous round's report claimed those literals were untouched; source-versus-materialized comparison disproved it. Content is now cut into tokens at the bytes a supported reference cannot contain — whitespace, the three quote characters, and the bracketing pairs — and a token qualifies only when a supported form starts at its beginning, after at most one leading escape and one shell assignment prefix (`NAME=` or `--flag=`), which is what keeps `--file=skills/...` working while a URL query keeps its bytes. Each of those inputs is now a token that does not begin with a supported form, so each is preserved without an alphabet of exceptions to maintain.
- Resolve the bundled helper a rule requires, for issue #92. Rebasing reached skill files only, so all three realized FFA rule hosts still named `skills/frequent-flyer-advocate/scripts/letter-fit.py`, which does not exist from the consumer directory — and that rule requires the script to measure a letter and forbids any other count, so the unresolved path was operational rather than prose. A rule body now resolves against the installed tree of the adapter that owns its instruction host: `.claude/skills` for a Claude-owned host, `.codex/skills` for a Codex-owned one, `.cursor/skills` for Cursor's own `.mdc`. When Claude and Codex share one host, every client reading it reaches the owner's tree by the same project-relative path, so the single contribution stays reachable to both and the rule body is still written once. The regression realizes Claude-only, Codex-only, Cursor, two separate hosts and one host reached through an import, and executes the command it extracts from each.
- Preserve a foreign include added to a generated-only instruction host, closing issue #93. Tessl adding `@AGENTS.md` plus its own prose to an ACR-created CLAUDE.md moves the rule contribution to AGENTS.md and omits CLAUDE.md; the compiler revisited an omitted target only when its prior ownership was shared, so the previously generated-only host fell to the engine's plain delete path and every later `check` and `realize` exited 4 with `CLAUDE.md: refusing to remove modified managed output`. The managed block was intact and only foreign bytes had been added. An omitted target whose ledger entries are homogeneously managed blocks is now revisited whichever ownership it held, which produces the hash-bound preservation proof the planner already accepted for the shared case: the foreign bytes survive, ownership drops to unmanaged, the contribution lands once on the new host, and re-realizing is a no-op. A plain generated file is deliberately still excluded — the engine owns its whole content and its removal path was already right — and an omitted host whose managed block was actually edited still refuses.
- Rebase a skill reference as a whole path for issue #92, so the commands a realized skill instructs an agent to run actually resolve. The first persistent FFA adoption through Homebrew ACR 0.1.3 installed cleanly, `acr check` reported current, and every helper command in the realized skills pointed at a file that does not exist: rebasing replaced the package-relative substring anywhere it appeared, so a legacy Tessl-installed path became `.tessl/plugins/jbaruch/frequent-flyer-advocate/.claude/skills/acr__jbaruch__ffa-acr-dogfood__frequent-flyer-advocate/scripts/letter-fit.py` — a native directory spliced into the middle of the path it was supposed to replace. The rewriting was self-consistent, which is why nothing caught it; the package's own tests asserted the mount path its source shipped with, and ACR's tests asserted the rule rather than the outcome. A reference is now rewritten only where it starts a path, so an interior segment, a URL and another package's tree keep their bytes, and the two forms ACR does support — the package-root path and `.tessl/plugins/<workspace>/<package>/` followed by that same path — are each replaced whole. The Tessl identity is matched structurally rather than against the package source, because the dogfood package was republished as `jbaruch/ffa-acr-dogfood` while its files still name the `jbaruch/frequent-flyer-advocate` identity they were written against, and no package-specific string would generalize. Rebasing also stopped at the skill it was rendering, so the router skill's commands, which invoke the advocate skill's tracker, were left addressing the Tessl tree; every renderer now applies its package's whole skill set, nested trees before the ancestors that contain them. The regression is the composition rather than the rule: a two-skill fixture is realized for all three adapters, every command its realized `SKILL.md` files instruct an agent to run is executed from the project directory with the argument it carries, and its unsupported references are held byte-for-byte alongside a companion file that must not change at all. What stays outside the contract is written down in [`docs/adapters.md`](docs/adapters.md) rather than guessed at: a reference ACR cannot map is preserved rather than rewritten into a path that resolves nowhere, and rule bodies are not rebased at all because they render into the shared instruction block, which owns no installed tree.
- Name the outputs an unapplied change would write. `acr check` exited `3` reporting only how many changes were pending, which told the reader a number and gave no way to find out which files it meant; the refusal now names the affected paths, bounded at five so a badly drifted project still produces a diagnostic worth reading to the end. A ledger-only change has no path and keeps the count-only form. A drift that is both files and ledger names the ledger after the paths, so the count and the list it introduces describe the same set.

- Accept GitHub's PAX metadata in source archives for issue #79, so `acr install github:OWNER/REPO` works against real GitHub for the first time. Every repository tarball GitHub serves begins with a global PAX header entry named `pax_global_header` carrying the commit id; root detection counted it as a second package root and refused the archive before reading any package content, and no producer could work around a record GitHub adds. PAX metadata entries are now skipped during enumeration while still counting against the entry bound, so resource accounting is unchanged and the link, duplicate, traversal, and special-file guards are untouched. The regression fixture for issue #80 builds the real GitHub shape programmatically — global header, per-file extended header, both, and neither — rather than committing a binary archive; a metadata-only archive still refuses as empty. Found minutes after 0.1.1 shipped by the first end-to-end dogfood, which copied a real Tessl plugin, converted it, published `github:jbaruch/ffa-acr-dogfood@v0.9.38`, and could not install it.
- Normalize extracted package file modes to the 0644 / 0755 pair every publisher records. With the PAX refusal gone, the first real install reached the next universal blocker: `release metadata content hash mismatch`. GitHub serves source tarballs with group-write permissions (0664 and 0775) that no publisher writes, and `internal/tarball`, `internal/publish`, and the vendor file records all reduce a mode to the executable-or-not pair, so a content hash computed from a source tree could never equal the hash in the release metadata. Extraction now applies the same rule: the recorded FFA source archive hashes to the published `sha256:91328738a779e5fe4c330b1f3dac8ef6a3af7091642caa8f981b8c1e6d7f1208`, and realization no longer carries a group-write bit into native output. Found by rerunning the dogfood install against the fixed binary; it has no issue of its own because nothing had ever read a real GitHub archive far enough to see it.
- Name the roots a multi-root refusal actually saw for issue #81. The refusal read `publish one package root`, a remedy that cannot help: a genuine second root is a property of the downloaded archive, and the producer may already publish exactly one. It now names both roots and points at the archive and the source it was built from.
- Match each `acr init` answer hint to what its question accepts for issue #82. The hint chose its wording from whether a default existed rather than from how many options an answer may name, so the agent question accepted a comma-separated list while advertising one answer, and the freshness question accepted one policy while advertising several. Both real answers worked, so the prompt was wrong in two directions at once. Arity now comes from the question's kind and the Enter shortcut from its defaults, which are independent properties.
- Say when `acr outdated` has nothing to check for issue #85. A project with no declarations reported `All latest dependencies are current.`, which is vacuously true and reads as a positive result — in the dogfood it appeared immediately after a failed install, where it looked like confirmation the install had worked. The renderer now distinguishes an empty project, an all-pinned or vendored one, and a genuinely checked-and-current one. `Service.Outdated` keeps its signature and the JSON stays `{"outdated": [...]}`, so the session-start checker is untouched.
- Transition a final shared-Markdown removal to unmanaged ownership for issue #55. Removing the last ACR-managed block produced a candidate that still claimed shared ownership, which the realization planner refuses, so uninstall failed on `AGENTS.md` and `CLAUDE.md` with `final shared-target removal must transition to unmanaged ownership` and the lifecycle stopped at a successful install. The compiler now makes the same transition `compileConfig` already made for the equivalent shape, and the shared-file goldens apply their removal through the realization engine instead of writing the compiler's intents by hand; the recorded trees are unchanged.

## 0.1.1 — 2026-09-04

### Changed

- Rename the Homebrew tap for issue #59 to `jbaruch/agentic-context-registry`, backed by `jbaruch/homebrew-agentic-context-registry`, so the install command names the project once as `brew install jbaruch/agentic-context-registry/acr`.

## 0.1.0 — 2026-09-04

### Added

- Document the complete MVP adoption journey for issue #3: executable offline command examples, per-command safety and rollback contracts, shared-file preservation goldens, structured troubleshooting, supported-adapter parity, Tessl consumer migration and producer dual-publishing guidance, plus command/code/link drift checks.
- Add `acr uninstall SOURCE [--dry-run]` and the interactive setup flow for issue #13. Uninstall is a state prune plus the ordinary realization pass, never a second removal engine: it drops the declaration, its rollback hold, and its lock row, then re-renders the pruned state so the planner deletes a generated-only target the remaining packages no longer want and splices a shared one down to its other owners, both bound to the observed hash. Removal keys on the declaration rather than on a ledger `Entry.Source` match, because `freshness.HookPackage` contributes the session-start hook under this repository's own source and a source match would delete the hook of any project that also declares it. It accepts no `--agent` and realizes across the union of the declared agents and every agent the ledger records, so a narrowed selection cannot leave another agent's outputs carrying entries for a package that is gone. A remaining package that cannot be materialized exits `1` with `remaining_packages_unavailable` before any file is planned; only the last dependency uninstalls fully offline, which a package cache would fix outside this issue. Setup adds `acr init` and the first-install questions in one outermost `internal/setupapp` decorator that holds the only `Prompter`, so `internal/dependency` gains no prompt-aware code, no `io.Reader`, and no new flag; the stored selection is what `init` pre-selects on a configured project and detection only contributes candidates, absence of `agents.yaml` rather than an empty `agents` list triggers the first-install questions, an empty selection is refused as `no_agent_selected` in every mode, and a rollback install asks the three-option `--hold`/`--pin`/cancel question whose first attempt is free because the install service refuses before it resolves anything. `--json`, `--non-interactive`, a non-terminal stdin, and a closed stdin all skip every question and take the typed refusal; the one terminal probe lives in `cmd/acr` and is true only for a real terminal, because a character-device test alone is not that test. The Go runtime opens `/dev/null` into a closed descriptor 0 before `main` and `/dev/null` carries `os.ModeCharDevice`, so a character-device probe reads a process started with stdin closed as a terminal and asks a question nobody can answer. The probe issues the stdlib termios ioctl per OS — `syscall.TIOCGETA` on darwin, `syscall.TCGETS` on linux, non-interactive on every platform without one — behind an `os.SameFile` comparison against `/dev/null`, and every error along the way reports non-interactive rather than fatal. End of input cancels a question before its line is parsed, so a preselected agent set or the preselected `outdated` never stands in for an answer nobody gave and a partial line without its newline is not a submitted one; a declined init question exits `2` with `setup_cancelled` having written nothing, and `acr init --non-interactive` on a configured project returns the stored selection before detection runs, so a malformed detected agent file cannot fail an invocation with nothing to decide. After `agents.yaml` and `.agents/registry.lock`, uninstall's previous-ledger path invariant names `.git/info/exclude` as its sole non-state exception: the `# BEGIN ACR GENERATED OUTPUTS` block is ACR's own local metadata rather than a package output, so removing a generated-only target also prunes that target's pattern from the block, while the file itself and every byte outside the block stay untouched.
- Add `acr migrate tessl` coexistence migration for issue #2: explicit manifest/file/CLI package mappings, Tessl-version-to-release-tag resolution, effective rule/skill/hook comparison, ownership and finalization-readiness reporting, ACR-only realization that byte-freezes Tessl state, duplicate-effect and gitignored-state diagnostics, and a parsed non-writing `--finalize` handoff for issue #8. Realization now uses a schema-versioned, fsynced before-image journal and non-blocking project claim so native output, `agents.yaml`, and `.agents/registry.lock` recover together before replanning after interruption, and `tessl_owned_target` refuses an ACR plan aimed at Tessl-owned bytes at exit `4`. A verified content cache that can eliminate immutable archive downloads on converged reruns is tracked in [issue #50](https://github.com/jbaruch/agentic-context-registry/issues/50).
- Add issue #8's reproducible local vendoring for unmapped Tessl packages and journaled `acr migrate tessl --finalize`: vendor locks are realized offline, explicit GitHub mappings can supersede equivalent vendor sources, and finalization removes only positively identified Tessl output after equivalence and Git-tracking gates while reporting retained content, rollback hashes, ledger re-anchors, and tracked stale references.
- Add `acr migrate tessl --dry-run` Tessl inventory for issue #1: read-only classification of installed plugin.json and tile.json packages onto the logical artifact model, preserved unmanaged spans, and a schema-versioned text/JSON report.
- Add temporary rollback holds for `latest` dependencies for issue #17: a reviewable `hold` on the declaration (known-good pin, rejected release, optional reason) and the rejected release's resolved identity on the lock; the real `HoldPolicy` filling #16's seam, which `acr install`, `acr update`, and the session-start `install` policy all consult so none can reinstall a rejected release; the `--hold` / `--pin` downgrade choice, required for every explicit reference that does not move a `latest` declaration forward, equality included, with a typed `downgrade_choice_required` usage error when neither is supplied and neither flag accepted over a standing hold unless the reference is proven not to advance the held release; `acr resume SOURCE` with `--dry-run` as the only path back to `latest`, alongside the explicit `--pin` conversion; `update` / `held` / `beyond-barrier` classification in `acr outdated` with held rows suppressed at session start; and a schema bump to version 2 with in-memory upgrade from version 1, with a hold recorded under version 1 refused by the runtime and by both JSON Schemas, so an older binary refuses a held file instead of silently dropping the hold.
- Add deterministic signed `acr` binaries for macOS and Linux, release version and source-commit reporting, checksums, CycloneDX SBOM and provenance verification, immutable tag release automation, tested Homebrew distribution, and verified install documentation for issue #15.
- Add the configurable `outdated`, `install`, and `none` session-start freshness policy, synthetic cross-agent hook realization, canonical 24-hour throttle state and project lock, fail-open CLI notices, and the rollback-hold seam for issue #16.
- Add `acr publish`, deterministic tagged-tree archives, schema-versioned release metadata, checksums, adapter realization gates, distinct Git-access failure codes, immutable draft-first GitHub Release uploads, consumer metadata verification, and a reusable publishing workflow for issue #9.
- Add `acr migrate tessl-plugin` to convert Tessl `tile.json` and `.tessl-plugin/plugin.json` packages into `agent-plugin.yaml` without rewriting artifact source files, for issue #11.
- Add production Claude Code, Codex, and Cursor adapters, persisted adapter selection, native hook/rule/skill/script layouts with invocation-safe skill paths, preservation-aware `realize`/`check` wiring, and cross-agent lifecycle goldens for issue #12.
- Add preservation-safe instruction include management for issue #6: deterministic include-graph discovery, byte-spliced managed Markdown blocks, surgical JSON/TOML merges, content-based ownership classification, and proof-bound promotion, demotion, and removal.
- Define the v1 `agent-plugin.yaml` contract, deterministic validator, package file enumeration, JSON Schema, and complete and minimal examples for issue #4.
- Add the `acr` command framework, stable process and output contracts, CLI reference, and macOS/Linux cross-build matrix for issue #13.
- Add deterministic `agents.yaml` dependency declarations, immutable registry locks, authenticated GitHub release/commit resolution, archive validation and content hashing, and install/list/outdated/update operations for issue #5.
- Add the adapter-neutral transactional realization planner, versioned ownership ledger, rollback-safe apply/check modes, and ownership-aware local Git exclusion management for issue #7.
- Add the versioned `internal/adapter` boundary: the read-only `Adapter` interface, data-only `Output` kinds, `compileOutputs` as the sole trusted bridge to `realize.Intent`, the capability preflight (`unsupported_adapter_capability`), the `Coordinator` library, and the `internal/adaptertest` golden-fixture harness with a reference fixture adapter, for issue #10.

### Changed

- Publish the Homebrew formula for issue #59 with a repository-scoped write deploy key provisioned by `scripts/provision-tap-deploy-key.sh`, replacing the personal access token requirement and preserving a working credential throughout rotation.
- Remove the browser-download installation steps from the macOS guidance in PR #68 after Apple notarization was dropped from the MVP in [issue #39](https://github.com/jbaruch/agentic-context-registry/issues/39), and recommend Homebrew on macOS and Linux as the only installation path with an upgrade route.

### Fixed

- Send release assets to `uploads.github.com` with a matching HTTP authority for issue #15, surface plain-text GitHub API failure bodies, and reserve unchanged-request retry advice for server failures.
- Keep `acr realize --agent` and `acr check --agent` a pure subset override for issue #45. `realizeapp.Service.Run` built intents for the selected adapters only while handing the planner the full previous ledger, so every omitted agent's target became unwanted: `planOmitted` deleted the generated-only ones and conflicted the shared ones, and `acr check --agent` reported the same phantom changes, none of which `docs/cli.md` promised. `realizeapp.splitLedger` now partitions the previous ledger by the invocation's agents, the planner compares against the selected agents' targets alone, and the `ModeApply` finalizer restores the rest through the new `realize.MergeLedgers`. A target owned by both fails closed at exit `4` naming `--agent` rather than dropping ownership entries either way; today's disjoint adapter trees make that unreachable from realization, but a hand-edited ledger can still contain one. An empty `--agent` list still means the persisted selection, which is the project's whole desired set, so deselecting an agent in `agents.yaml` keeps removing what it left behind. Planning gained an optional retained ledger for the omitted agents' targets: without it the local Git exclusion block would be rewritten from the scoped ledger alone and un-exclude another agent's generated outputs, which would also make `check --agent` report changes in any Git worktree. `acr uninstall`'s covered-agent union is then redundancy rather than the only guard.
- Refuse an undeclared `SOURCE` as a usage error across `acr uninstall`, `acr resume`, and `acr update` for issue #13: all three share one typed `*dependency.NotDeclaredError` mapped to exit `2` `dependency_not_declared`, replacing `resume` and `update`'s exit `1` `dependency_operation_failed`. The message names `acr list` rather than `acr install`, because the operator supplied a source that is not there and the next command is the one that shows what is. `TestServiceUpdateRejectsUndeclaredSource` asserted the old install guidance and moves to the typed refusal with it.
- Fail `acr migrate tessl --dry-run` when tessl.json names a dependency that is not a two-segment workspace/package identity, for issue #1.
- Fail `acr migrate tessl --dry-run` when `.tessl/RULES.md` includes a non-package-relative rules path, and mark two plugin.json declarations that share an artifact ID as ambiguous, for issue #1.
- Fail `acr migrate tessl --dry-run` when a Tessl manifest declares a non-package-relative rule or skill path, or when a native JSON/TOML config cannot be decoded, instead of reading outside the package or omitting user content, for issue #1.
- Fail `acr migrate tessl --dry-run` when a declared skill's `SKILL.md` exists but cannot be read, instead of classifying it as `missing-skill`, for issue #1.
- Recognize the Tessl hook dispatcher only at the head of the command in the issue #1 inventory, so a user hook that quotes `tessl hook run` in an argument or a shell comment stays preserved.
- Report orphan Tessl native files under adapter rule and skill roots as `unmapped` with reason `orphan-tessl-native` in the issue #1 inventory, instead of leaving a partial uninstall or stale native tree silent.
- Keep a Tessl-declared skill in the issue #1 inventory when its `SKILL.md` is absent, instead of expanding sibling directories as phantom skills.
- Treat a user hook that names a `.tessl/` path as preserved in the issue #1 inventory; Tessl ownership of native hook commands is the dispatcher literal, not a path substring.
- Keep Tessl package files and `.tessl/RULES.md` out of `preserved` so issue #1's inventory reports them as artifacts or unmapped, never as unmanaged user content.
- Fail `acr migrate tessl --dry-run` when the include-graph snapshot cannot be read instead of printing a partial inventory for issue #1.
- Print Tessl inventory failures with a single CLI recovery sentence for issue #1.
- Treat a rule declared by only one of `plugin.json` and a stale `tile.json` as migratable, not ambiguous, in the issue #1 inventory.
- Reject malformed Tessl hook commands when computing plugin-tree ownership so they cannot hide undeclared files from `unmapped` in issue #1.
- Keep an unsupported duplicate Tessl skill classified as unsupported rather than downgrading it to ambiguous in issue #1.
- Report symlinked entries under `.tessl/plugins/**` as `unmapped` with reason `plugin-symlink` so issue #8 can vendor them.
- Omit the always-empty per-package `unmapped` group from the issue #1 Tessl inventory text report.
- Classify em-dash-free `applyTo:` on always-on rules as prose instead of globs for issue #11.
- Include populated unmapped reports in producer-migration JSON and text refusals for issue #11.
- Preserve scalar fields declared by only one Tessl manifest, including tile-only description, repository, and provenance, for issue #11.
- Detect `nativeHooks` agent widening by adapter membership rather than set size, so a three-agent declaration that omits an ACR adapter is `agent_widening`, for issue #11.
- Refuse conversion when tile.json and plugin.json disagree on `private`, and treat `private: true` on either manifest as `unmapped_field`, for issue #11.
- Treat a `rules` or `skills` set declared by only one Tessl manifest as `ambiguous_manifest` instead of dropping the other side's artifacts, for issue #11.
- Report both halves of an `alwaysApply: true` plus `applyTo:` rule, surface the refused Tessl field on `--json` errors, and reject parent-directory segments in declared rule paths as `invalid_path`, for issue #11.
- Report the invocation's dry-run flag on producer-migration JSON refusals for issue #11.
- Emit `ignored` as an empty array rather than null on successful producer-migration JSON for issue #11.
- Write producer-migration manifests through a package-local temporary file and rename, so a failed write cannot leave a partial `agent-plugin.yaml` that later reports `manifest_conflict`, for issue #11.
- Classify a missing or non-canonical `--project` path as a freshness project-state failure instead of a state-home failure, for issue #35.
- Surface a `freshness_update_failed` notice when `--policy none` skips a check against unreadable project state, for issue #35.
- Reject fabricated preservation fragments that occur only in rendered output, closing a planner path that could replace a shared target's observed unmanaged content in issue #30.
- Route malformed or unreachable project state through freshness fail-open attempts that record throttle state, keep JSON application errors to one parseable error object when notices accompany them, document future-timestamp recovery, and preserve a re-declared dependency's current requested policy when a rollback hold keeps its existing lock in issue #35.
- Reject symlinked manifests and Windows-prefixed artifact paths before reading package content, keep rule glob validation aligned with JSON Schema, and avoid derived repository diagnostics for invalid package names in issue #21.
- Reject non-canonical GitHub source URLs independently of package-name validation in issue #23.
