# Changelog

## Next

### Added

- Cover every public CLI command with a runnable journey, and make an uncovered command fail the suite. The dogfood that produced the previous entries found a universal install failure no test could have caught: nothing exercised a real GitHub source archive through the actual argument parser and production composition, so every command's "coverage" was a mix of service-level tests and empty-project smoke runs that pass whether or not the command does anything. `cmd/acr` now carries one journey table. Each entry runs real argv through the shipped `runWith` composition against a stateful GitHub fixture — releases, commits, tarball and asset redirects, uploads and readback — reached through the production `GitHubClient` at its production hostnames, so the client keeps building, authenticating, redirect-checking and origin-validating exactly the requests it builds against GitHub. Source tarballs carry the leading `pax_global_header` and the group permissions codeload actually serves, so the two failures the dogfood found stay covered by shape rather than by anecdote. `TestCLIJourneyInventory` then holds the completed journeys against `cli.Leaves()`: every one of the 15 executable leaves needs a successful outcome and a refusal, and a journey registers coverage only after asserting a positive result, so a help-only or empty-state test satisfies nothing. The inventory enumerates the dispatch registry `commandFor` accepts rather than the display order, because a first attempt derived it from `commandOrder` and a command added to `commandSpecs` alone was then dispatchable, undocumented and uncovered all at once — the gate now runs against the production surface with exactly that command added and has to name it, alongside `TestCLIJourneyInventoryFailsForTheIntendedCause`, which mutates a synthetic inventory the same ways — a new command, a new subcommand, a removed journey, a leaf that only refuses — and requires a complaint by name for each. Where a journey reasons about elapsed time it holds the clock still: `freshnessapp.NewApplication` takes an optional clock that `publishapp`, `migrateapp` and `runWith` forward untouched, so the throttle journey crosses Window-1s and Window on purpose instead of passing because the suite is fast, and without an option the composition is byte-for-byte the one the binary ships. The publish journey is the whole arc: a real tagged Git repository is published through the boundary and a consumer that has never seen it installs, realizes and checks the release that publishing created, with no post-publication state seeded by hand. Three lanes are recorded separately because they establish different things: the composed in-process stack, a composed subprocess with real descriptors and exit statuses, and the compiled shipped binary for local and offline behavior. What none of them establish — live GitHub conformance, real agent loading, fixture provenance capture — is written down in [`docs/testing.md`](docs/testing.md) rather than implied to be covered, and the two checks that would establish the first two are runnable procedures rather than intentions: [`docs/manual-conformance.md`](docs/manual-conformance.md) carries the read-only binary lifecycle check and the per-agent canary, each with its isolation, the identities to record, the nonempty observations a pass requires and a pass/fail/blocked classification, on a monthly plus contract-change cadence. The canary needed a fixture rather than a paragraph: the first draft sent the operator to the lifecycle check's own scratch consumer, which that check uninstalls twice and then deletes and which only ever selected two of the three adapters, so following the stated order left nothing installed to observe. The canary now has its own consumer and its own local package — [`docs/canary-package/`](docs/canary-package) carries an always-on rule, a rule scoped to `docs/**`, a discoverable skill and a session-start hook that appends one marker line to a scratch log and does nothing else, vendored through `acr migrate tessl` so no publication and no network are involved. The phases are numbered with the observations before the teardown, each observation names how it is captured and what makes it a failure, and `cmd/acr/canary_test.go` executes every phase except starting the agents, so what stays manual is the observation rather than the setup. [`cmd/acr/fixture-provenance.md`](cmd/acr/fixture-provenance.md) sits beside the builders and separates what was observed from a real GitHub tarball — endpoint, observation date, capture digest, PAX header, root, mode sets, ordered entries — from the response bodies written from the API contract, names the capture fields nobody kept, distinguishes the capture digest from the published asset digest and the canonical content hash, and states the same refresh cadence and its reviewed structural diff.
- Answer `acr init` over a real pseudo-terminal in the journeys. The interactive path was previously proven only by the probe that decides it and by the refusals every non-terminal shape takes; a typed selection now travels through an actual terminal into `agents.yaml`, and an unparsable answer re-prompts instead of choosing something nobody asked for.
- Prove the credential discovery order end to end. `GH_TOKEN` before `GITHUB_TOKEN` before `gh auth token` before `git credential fill` was only ever exercised by injecting a token provider, which proves nothing about the order the commands are tried in or about the `GIT_TERMINAL_PROMPT=0` that keeps a helper from blocking. The order is now driven through real executables on a scratch `PATH`.

### Changed

- Make the executable command surface enumerable. The accepted subcommands of `freshness` and `migrate` were literals inside parser branches, so nothing outside the parser could list what a user is able to run, and the meta commands were a hard-coded switch plus two hard-coded help lines. Both are descriptors now, `cli.Leaves()` returns the 15 executable leaves the parser dispatches on and help renders, and the acceptance suite enforces coverage against that rather than against a hand-maintained list or a Go AST reader that any internal refactor would break. `NewGitHubClient` takes options so a test can supply an HTTP client while keeping the production API host, upload host and trusted redirect origins; without options it is the client the binary already shipped.
- Make the fake GitHub remote observable, closing issue #67. `PublishRelease` mutated a range-loop copy, so a published release stayed a draft in every stored representation; `DeleteRelease` logged the identifier and left the release visible to later lookups; `UploadAsset` never associated the asset with its release. A publisher-to-consumer journey through that double could only pass by seeding the outcome it was meant to prove. The live release index is now what every lookup reads and every publication method mutates, the creation and deletion slices are the event logs they were named for, and reads hand out copies so a caller editing a returned asset slice cannot corrupt storage. Repairing the mutations was not enough on its own: the `Latest` and `Releases` seeds still answered before the live index, so a release deleted through the publication API kept answering the consumer views seeded with it and a release published through the double stayed hidden behind an older `Latest` seed — one `Remote` could call the same release absent for a publisher and present for a consumer. Every lookup now resolves live index first, seed second and tombstone never; a publication advances what `LatestRelease` returns; a seeded release can be uploaded to and published, which promotes it into the live index; and the seeds a caller wrote stay where they were put. Unifying the views moved the remaining defect into identifier allocation, because latest selection and deletion then keyed on identifiers the counters handed out from fixed floors regardless of what a caller had seeded: a seeded release at 1001 collided with the next generated one, so deleting the new release tombstoned the seeded one; a seed at 5000 hid every later publication behind it; and an upload reused a seeded asset id, rewriting the bytes of an asset nobody uploaded to. Allocation now advances past the highest identifier visible anywhere — the live index, every seed, the assets those releases carry, the creation log and the tombstones, so a deleted identifier stays reserved — and no caller adjusts a counter and no fixture lowers an id to make a test pass.
- Stop hashing Git's own storage in documented-command snapshots, closing issue #76. Background maintenance wrote `.git/objects/maintenance.lock` while a documented command ran, and the before/after comparison failed for a reason the command under test had nothing to do with. The shared snapshot skips Git internals and records what the promises are made of instead — permission bits, symlink targets and directories alongside content digests — plus `.git/info/exclude`, which ACR owns a generated block in. Excluding the storage also hid the index and the refs, so a command could stage a mode change or move HEAD and every composed journey would still call the project unchanged; the snapshot now records what Git answers rather than what Git stores — the commit HEAD names, the branch, every index entry with its mode and blob, the working-tree status, and the exclude file resolved through Git so a linked worktree's common-directory copy is covered too. The probe stages a mode change and moves HEAD without touching a product byte, and both have to fail the snapshot while maintenance lock churn still must not.
- Supply the repository metadata stage 0 of the migration guide actually needs, for issues #83 and #84. Following the stage verbatim on a real Tessl plugin, the first prescribed `acr migrate tessl-plugin --dry-run` refused at `required`, because real manifests have no `repository` field and the guide never said to supply one. The stage now names the prerequisite up front and demonstrates `--repository` on both the rehearsal and the apply. It also told the operator to review the proposed `agent-plugin.yaml` before applying, which neither the text nor the JSON dry run emits — those bytes exist only once the conversion has run for real — so the review moved to where the file exists rather than growing a preview flag the tool does not need. The `producer` documentation fixture drops its repository field so the executable harness runs the walkthrough against the shape the guide claims to describe.

### Fixed

- Name the outputs an unapplied change would write. `acr check` exited `3` reporting only how many changes were pending, which told the reader a number and gave no way to find out which files it meant; the refusal now names the affected paths, bounded at five so a badly drifted project still produces a diagnostic worth reading to the end. A ledger-only change has no path and keeps the count-only form.

- Accept GitHub's PAX metadata in source archives for issue #79, so `acr install github:OWNER/REPO` works against real GitHub for the first time. Every repository tarball GitHub serves begins with a global PAX header entry named `pax_global_header` carrying the commit id; root detection counted it as a second package root and refused the archive before reading any package content, and no producer could work around a record GitHub adds. PAX metadata entries are now skipped during enumeration while still counting against the entry bound, so resource accounting is unchanged and the link, duplicate, traversal, and special-file guards are untouched. The regression fixture for issue #80 builds the real GitHub shape programmatically — global header, per-file extended header, both, and neither — rather than committing a binary archive; a metadata-only archive still refuses as empty. Found minutes after 0.1.1 shipped by the first end-to-end dogfood, which copied a real Tessl plugin, converted it, published `github:jbaruch/ffa-acr-dogfood@v0.9.38`, and could not install it.
- Normalize extracted package file modes to the 0644 / 0755 pair every publisher records. With the PAX refusal gone, the first real install reached the next universal blocker: `release metadata content hash mismatch`. GitHub serves source tarballs with group-write permissions (0664 and 0775) that no publisher writes, and `internal/tarball`, `internal/publish`, and the vendor file records all reduce a mode to the executable-or-not pair, so a content hash computed from a source tree could never equal the hash in the release metadata. Extraction now applies the same rule: the recorded FFA source archive hashes to the published `sha256:91328738a779e5fe4c330b1f3dac8ef6a3af7091642caa8f981b8c1e6d7f1208`, and realization no longer carries a group-write bit into native output. Found by rerunning the dogfood install against the fixed binary; it has no issue of its own because nothing had ever read a real GitHub archive far enough to see it.
- Name the roots a multi-root refusal actually saw for issue #81. The refusal read `publish one package root`, a remedy that cannot help: a genuine second root is a property of the downloaded archive, and the producer may already publish exactly one. It now names both roots and points at the archive and the source it was built from.
- Match each `acr init` answer hint to what its question accepts for issue #82. The hint chose its wording from whether a default existed rather than from how many options an answer may name, so the agent question accepted a comma-separated list while advertising one answer, and the freshness question accepted one policy while advertising several. Both real answers worked, so the prompt was wrong in two directions at once. Arity now comes from the question's kind and the Enter shortcut from its defaults, which are independent properties.
- Say when `acr outdated` has nothing to check for issue #85. A project with no declarations reported `All latest dependencies are current.`, which is vacuously true and reads as a positive result — in the dogfood it appeared immediately after a failed install, where it looked like confirmation the install had worked. The renderer now distinguishes an empty project, an all-pinned or vendored one, and a genuinely checked-and-current one. `Service.Outdated` keeps its signature and the JSON stays `{"outdated": [...]}`, so the session-start checker is untouched.
- Transition a final shared-Markdown removal to unmanaged ownership for issue #55. Removing the last ACR-managed block produced a candidate that still claimed shared ownership, which the realization planner refuses, so uninstall failed on `AGENTS.md` and `CLAUDE.md` with `final shared-target removal must transition to unmanaged ownership` and the lifecycle stopped at a successful install. The compiler now makes the same transition `compileConfig` already made for the equivalent shape, and the shared-file goldens apply their removal through the realization engine instead of writing the compiler's intents by hand; the recorded trees are unchanged.

## 0.1.1 — 2026-09-04

### Changed

- Rename the Homebrew tap for issue #59 to `jbaruch/agentic-context-registry`, backed by `jbaruch/homebrew-agentic-context-registry`, so the install command names the project once as `brew install jbaruch/agentic-context-registry/acr`.

## 0.1.0 — 2026-09-04

### Added

- Document the complete MVP adoption journey for issue #3: executable offline command examples, per-command safety and rollback contracts, shared-file preservation goldens, structured troubleshooting, supported-adapter parity, Tessl consumer migration and producer dual-publishing guidance, plus command/code/link drift checks.
- Add `acr uninstall SOURCE [--dry-run]` and the interactive setup flow for issue #13. Uninstall is a state prune plus the ordinary realization pass, never a second removal engine: it drops the declaration, its rollback hold, and its lock row, then re-renders the pruned state so the planner deletes a generated-only target the remaining packages no longer want and splices a shared one down to its other owners, both bound to the observed hash. Removal keys on the declaration rather than on a ledger `Entry.Source` match, because `freshness.HookPackage` contributes the session-start hook under this repository's own source and a source match would delete the hook of any project that also declares it. It accepts no `--agent` and realizes across the union of the declared agents and every agent the ledger records, so a narrowed selection cannot leave another agent's outputs carrying entries for a package that is gone. A remaining package that cannot be materialized exits `1` with `remaining_packages_unavailable` before any file is planned; only the last dependency uninstalls fully offline, which a package cache would fix outside this issue. Setup adds `acr init` and the first-install questions in one outermost `internal/setupapp` decorator that holds the only `Prompter`, so `internal/dependency` gains no prompt-aware code, no `io.Reader`, and no new flag; the stored selection is what `init` pre-selects on a configured project and detection only contributes candidates, absence of `agents.yaml` rather than an empty `agents` list triggers the first-install questions, an empty selection is refused as `no_agent_selected` in every mode, and a rollback install asks the three-option `--hold`/`--pin`/cancel question whose first attempt is free because the install service refuses before it resolves anything. `--json`, `--non-interactive`, a non-terminal stdin, and a closed stdin all skip every question and take the typed refusal; the one terminal probe lives in `cmd/acr` and is true only for a real terminal, because a character-device test alone is not that test. The Go runtime opens `/dev/null` into a closed descriptor 0 before `main` and `/dev/null` carries `os.ModeCharDevice`, so a character-device probe reads a process started with stdin closed as a terminal and asks a question nobody can answer. The probe issues the stdlib termios ioctl per OS — `syscall.TIOCGETA` on darwin, `syscall.TCGETS` on linux, non-interactive on every platform without one — behind an `os.SameFile` comparison against `/dev/null`, and every error along the way reports non-interactive rather than fatal. End of input cancels a question before its line is parsed, so a preselected agent set or the preselected `outdated` never stands in for an answer nobody gave and a partial line without its newline is not a submitted one; a declined init question exits `2` with `setup_cancelled` having written nothing, and `acr init --non-interactive` on a configured project returns the stored selection before detection runs, so a malformed detected agent file cannot fail an invocation with nothing to decide. After `agents.yaml` and `.agents/registry.lock`, uninstall's previous-ledger path invariant names `.git/info/exclude` as its sole non-state exception: the `# BEGIN ACR GENERATED OUTPUTS` block is ACR's own local metadata rather than a package output, so removing a generated-only target also prunes that target's pattern from the block, while the file itself and every byte outside the block stay untouched.
- Add `acr migrate tessl` coexistence migration for issue #2: explicit manifest/file/CLI package mappings, Tessl-version-to-release-tag resolution, effective rule/skill/hook comparison, ownership and finalization-readiness reporting, ACR-only realization that byte-freezes Tessl state, duplicate-effect and gitignored-state diagnostics, and a parsed non-writing `--finalize` handoff for issue #8. Realization now uses a schema-versioned, fsynced before-image journal and non-blocking project claim so native output, `agents.yaml`, and `.agents/registry.lock` recover together before replanning after interruption, and `tessl_owned_target` refuses an ACR plan aimed at Tessl-owned bytes at exit `4`. A verified content cache that can eliminate immutable archive downloads on converged reruns is tracked in [issue #50](https://github.com/jbaruch/agentic-context-registry/issues/50).
- Add issue #8's reproducible local vendoring for unmapped Tessl packages and journaled `acr migrate tessl --finalize`: vendor locks are realized offline, explicit GitHub mappings can supersede equivalent vendor sources, and finalization removes only positively identified Tessl output after equivalence and Git-tracking gates while reporting retained content, rollback hashes, ledger re-anchors, and tracked stale references.
- Add `acr migrate tessl --dry-run` Tessl inventory for issue #1: read-only classification of installed plugin.json and tile.json packages onto the logical artifact model, preserved unmanaged spans, and a schema-versioned text/JSON report.
- Add temporary rollback holds for `latest` dependencies for issue #17: a reviewable `hold` on the declaration (known-good pin, rejected release, optional reason) and the rejected release's resolved identity on the lock; the real `HoldPolicy` filling #16's seam, which `acr install`, `acr update`, and the session-start `install` policy all consult so none can reinstall a rejected release; the `--hold` / `--pin` downgrade choice, required for every explicit reference that does not move a `latest` declaration forward, equality included, with a typed `downgrade_choice_required` usage error when neither is supplied and neither flag accepted over a standing hold unless the reference is proven not to advance the held release; `acr resume SOURCE` with `--dry-run` as the only path back to `latest`, alongside the explicit `--pin` conversion; `update` / `held` / `beyond-barrier` classification in `acr outdated` with held rows suppressed at session start; and a schema bump to version 2 with in-memory upgrade from version 1, with a hold recorded under version 1 refused by the runtime and by both JSON Schemas, so an older binary refuses a held file instead of silently dropping the hold.
- Add deterministic signed `acr` binaries for macOS and Linux, release version and source-commit reporting, checksums, CycloneDX SBOM and provenance verification, immutable tag release automation, tested Homebrew distribution, and verified install documentation for issue #15.
- Add the configurable `outdated`, `install`, and `none` session-start freshness policy, synthetic cross-agent hook realization, canonical 24-hour throttle state and project lock, fail-open CLI notices, and the rollback-hold seam for issue #16.
- Add `acr publish`, deterministic tagged-tree archives, schema-versioned release metadata, checksums, adapter realization gates, distinct Git-access failure codes, immutable draft-first GitHub Release uploads, consumer metadata verification, and a reusable publishing workflow for issue #9.
- Add `acr migrate tessl-plugin` to convert Tessl `tile.json` and `.tessl-plugin/plugin.json` packages into `agent-plugin.yaml` without rewriting artifact source files, for issue #11.
- Add production Claude Code, Codex, and Cursor adapters, persisted adapter selection, native hook/rule/skill/script layouts with invocation-safe skill paths, preservation-aware `realize`/`check` wiring, and cross-agent lifecycle goldens for issue #12.
- Add preservation-safe instruction include management for issue #6: deterministic include-graph discovery, byte-spliced managed Markdown blocks, surgical JSON/TOML merges, content-based ownership classification, and proof-bound promotion, demotion, and removal.
- Define the v1 `agent-plugin.yaml` contract, deterministic validator, package file enumeration, JSON Schema, and complete and minimal examples for issue #4.
- Add the `acr` command framework, stable process and output contracts, CLI reference, and macOS/Linux cross-build matrix for issue #13.
- Add deterministic `agents.yaml` dependency declarations, immutable registry locks, authenticated GitHub release/commit resolution, archive validation and content hashing, and install/list/outdated/update operations for issue #5.
- Add the adapter-neutral transactional realization planner, versioned ownership ledger, rollback-safe apply/check modes, and ownership-aware local Git exclusion management for issue #7.
- Add the versioned `internal/adapter` boundary: the read-only `Adapter` interface, data-only `Output` kinds, `compileOutputs` as the sole trusted bridge to `realize.Intent`, the capability preflight (`unsupported_adapter_capability`), the `Coordinator` library, and the `internal/adaptertest` golden-fixture harness with a reference fixture adapter, for issue #10.

### Changed

- Publish the Homebrew formula for issue #59 with a repository-scoped write deploy key provisioned by `scripts/provision-tap-deploy-key.sh`, replacing the personal access token requirement and preserving a working credential throughout rotation.
- Remove the browser-download installation steps from the macOS guidance in PR #68 after Apple notarization was dropped from the MVP in [issue #39](https://github.com/jbaruch/agentic-context-registry/issues/39), and recommend Homebrew on macOS and Linux as the only installation path with an upgrade route.

### Fixed

- Send release assets to `uploads.github.com` with a matching HTTP authority for issue #15, surface plain-text GitHub API failure bodies, and reserve unchanged-request retry advice for server failures.
- Keep `acr realize --agent` and `acr check --agent` a pure subset override for issue #45. `realizeapp.Service.Run` built intents for the selected adapters only while handing the planner the full previous ledger, so every omitted agent's target became unwanted: `planOmitted` deleted the generated-only ones and conflicted the shared ones, and `acr check --agent` reported the same phantom changes, none of which `docs/cli.md` promised. `realizeapp.splitLedger` now partitions the previous ledger by the invocation's agents, the planner compares against the selected agents' targets alone, and the `ModeApply` finalizer restores the rest through the new `realize.MergeLedgers`. A target owned by both fails closed at exit `4` naming `--agent` rather than dropping ownership entries either way; today's disjoint adapter trees make that unreachable from realization, but a hand-edited ledger can still contain one. An empty `--agent` list still means the persisted selection, which is the project's whole desired set, so deselecting an agent in `agents.yaml` keeps removing what it left behind. Planning gained an optional retained ledger for the omitted agents' targets: without it the local Git exclusion block would be rewritten from the scoped ledger alone and un-exclude another agent's generated outputs, which would also make `check --agent` report changes in any Git worktree. `acr uninstall`'s covered-agent union is then redundancy rather than the only guard.
- Refuse an undeclared `SOURCE` as a usage error across `acr uninstall`, `acr resume`, and `acr update` for issue #13: all three share one typed `*dependency.NotDeclaredError` mapped to exit `2` `dependency_not_declared`, replacing `resume` and `update`'s exit `1` `dependency_operation_failed`. The message names `acr list` rather than `acr install`, because the operator supplied a source that is not there and the next command is the one that shows what is. `TestServiceUpdateRejectsUndeclaredSource` asserted the old install guidance and moves to the typed refusal with it.
- Fail `acr migrate tessl --dry-run` when tessl.json names a dependency that is not a two-segment workspace/package identity, for issue #1.
- Fail `acr migrate tessl --dry-run` when `.tessl/RULES.md` includes a non-package-relative rules path, and mark two plugin.json declarations that share an artifact ID as ambiguous, for issue #1.
- Fail `acr migrate tessl --dry-run` when a Tessl manifest declares a non-package-relative rule or skill path, or when a native JSON/TOML config cannot be decoded, instead of reading outside the package or omitting user content, for issue #1.
- Fail `acr migrate tessl --dry-run` when a declared skill's `SKILL.md` exists but cannot be read, instead of classifying it as `missing-skill`, for issue #1.
- Recognize the Tessl hook dispatcher only at the head of the command in the issue #1 inventory, so a user hook that quotes `tessl hook run` in an argument or a shell comment stays preserved.
- Report orphan Tessl native files under adapter rule and skill roots as `unmapped` with reason `orphan-tessl-native` in the issue #1 inventory, instead of leaving a partial uninstall or stale native tree silent.
- Keep a Tessl-declared skill in the issue #1 inventory when its `SKILL.md` is absent, instead of expanding sibling directories as phantom skills.
- Treat a user hook that names a `.tessl/` path as preserved in the issue #1 inventory; Tessl ownership of native hook commands is the dispatcher literal, not a path substring.
- Keep Tessl package files and `.tessl/RULES.md` out of `preserved` so issue #1's inventory reports them as artifacts or unmapped, never as unmanaged user content.
- Fail `acr migrate tessl --dry-run` when the include-graph snapshot cannot be read instead of printing a partial inventory for issue #1.
- Print Tessl inventory failures with a single CLI recovery sentence for issue #1.
- Treat a rule declared by only one of `plugin.json` and a stale `tile.json` as migratable, not ambiguous, in the issue #1 inventory.
- Reject malformed Tessl hook commands when computing plugin-tree ownership so they cannot hide undeclared files from `unmapped` in issue #1.
- Keep an unsupported duplicate Tessl skill classified as unsupported rather than downgrading it to ambiguous in issue #1.
- Report symlinked entries under `.tessl/plugins/**` as `unmapped` with reason `plugin-symlink` so issue #8 can vendor them.
- Omit the always-empty per-package `unmapped` group from the issue #1 Tessl inventory text report.
- Classify em-dash-free `applyTo:` on always-on rules as prose instead of globs for issue #11.
- Include populated unmapped reports in producer-migration JSON and text refusals for issue #11.
- Preserve scalar fields declared by only one Tessl manifest, including tile-only description, repository, and provenance, for issue #11.
- Detect `nativeHooks` agent widening by adapter membership rather than set size, so a three-agent declaration that omits an ACR adapter is `agent_widening`, for issue #11.
- Refuse conversion when tile.json and plugin.json disagree on `private`, and treat `private: true` on either manifest as `unmapped_field`, for issue #11.
- Treat a `rules` or `skills` set declared by only one Tessl manifest as `ambiguous_manifest` instead of dropping the other side's artifacts, for issue #11.
- Report both halves of an `alwaysApply: true` plus `applyTo:` rule, surface the refused Tessl field on `--json` errors, and reject parent-directory segments in declared rule paths as `invalid_path`, for issue #11.
- Report the invocation's dry-run flag on producer-migration JSON refusals for issue #11.
- Emit `ignored` as an empty array rather than null on successful producer-migration JSON for issue #11.
- Write producer-migration manifests through a package-local temporary file and rename, so a failed write cannot leave a partial `agent-plugin.yaml` that later reports `manifest_conflict`, for issue #11.
- Classify a missing or non-canonical `--project` path as a freshness project-state failure instead of a state-home failure, for issue #35.
- Surface a `freshness_update_failed` notice when `--policy none` skips a check against unreadable project state, for issue #35.
- Reject fabricated preservation fragments that occur only in rendered output, closing a planner path that could replace a shared target's observed unmanaged content in issue #30.
- Route malformed or unreachable project state through freshness fail-open attempts that record throttle state, keep JSON application errors to one parseable error object when notices accompany them, document future-timestamp recovery, and preserve a re-declared dependency's current requested policy when a rollback hold keeps its existing lock in issue #35.
- Reject symlinked manifests and Windows-prefixed artifact paths before reading package content, keep rule glob validation aligned with JSON Schema, and avoid derived repository diagnostics for invalid package names in issue #21.
- Reject non-canonical GitHub source URLs independently of package-name validation in issue #23.
